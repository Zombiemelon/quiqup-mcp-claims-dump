---
phase: 02-integrations
plan: 06
type: execute
wave: 6
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - evals/datasets/integrations-shared-v1.ts
  - evals/integrations-shared.ts
  - evals/score-integrations-shared.ts
  - evals/datasets/shopify-integration-v1.ts
  - evals/shopify-integration.ts
  - evals/score-shopify-integration.ts
  - evals/datasets/woocommerce-integration-v1.ts
  - evals/woocommerce-integration.ts
  - evals/score-woocommerce-integration.ts
  - evals/datasets/salla-integration-v1.ts
  - evals/salla-integration.ts
  - evals/score-salla-integration.ts
  - evals/datasets/destructive-integrations-v1.ts
  - evals/destructive-integrations.ts
  - evals/score-destructive-integrations.ts
  - package.json
  - .github/workflows/eval-gate.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "A Langfuse eval exists for the shared-integrations family (anchored on list_integration_connections + repair_integration_orders + confirm_ff_export) and passes the args-overlap gate."
    - "A Langfuse eval exists for the Shopify family (anchored on get_shopify_config + update_shopify_config + setup_shopify_callback) and passes args-overlap + description-quality gates."
    - "A Langfuse eval exists for the WooCommerce family (anchored on list_woocommerce_states + upsert_woocommerce_config) and passes args-overlap + description-quality gates (with the Quiqup-vs-WooCommerce state disambiguation scorer)."
    - "A Langfuse eval exists for the Salla family (anchored on get_salla_connection + get_salla_config + update_salla_config + toggle_salla_fulfillment) and includes a STATIC token-omission scorer that reads lib/tools/get-salla-connection.ts source text and asserts no `text.includes(token)` leak (mirroring the auth-isolation pattern from plan 01-04 Task 2 Step B)."
    - "A Langfuse eval exists for the destructive-integrations family (anchored on delete_integration_source + delete_salla_connection) and includes a STATIC confirm-gate scorer that imports the spec inputSchema and asserts both tools have a `confirm` field via destructiveConfirmField — proves the gate cannot be silently removed."
    - "All 5 new evals are runnable via `pnpm eval:integrations-shared` / `eval:shopify-integration` / `eval:woocommerce-integration` / `eval:salla-integration` / `eval:destructive-integrations`."
    - "The CI eval-gate workflow runs all 5 new evals with EVAL_GATE=1 and fails the build on regression."
    - "Each eval imports `spec.description` and `spec.inputSchema` directly from the production module — no inline string copies (mirrors the drift-proofing pattern established in plan 01-04 T-01-26)."
  artifacts:
    - path: "evals/datasets/integrations-shared-v1.ts"
      provides: "Dataset for the shared-integrations family (5 tools)"
      exports: ["items", "TODAY"]
    - path: "evals/integrations-shared.ts"
      provides: "Langfuse experiment runner for the shared-integrations family"
      contains: "experiment.run"
    - path: "evals/score-integrations-shared.ts"
      provides: "Evaluators (tool-name-match, required-fields-present, args-overlap, description-quality)"
      exports: ["evaluators"]
    - path: "evals/datasets/shopify-integration-v1.ts"
      provides: "Dataset for the Shopify family (6 tools)"
      exports: ["items"]
    - path: "evals/shopify-integration.ts"
      provides: "Langfuse experiment runner for the Shopify family (includes the single-use OAuth-code description assertion)"
      contains: "experiment.run"
    - path: "evals/score-shopify-integration.ts"
      provides: "Evaluators including the single-use + sensitive-token description-quality scorers"
      exports: ["evaluators"]
    - path: "evals/datasets/woocommerce-integration-v1.ts"
      provides: "Dataset for the WooCommerce family (6 tools)"
      exports: ["items"]
    - path: "evals/woocommerce-integration.ts"
      provides: "Langfuse experiment runner for the WooCommerce family"
      contains: "experiment.run"
    - path: "evals/score-woocommerce-integration.ts"
      provides: "Evaluators including the Quiqup-vs-WooCommerce state disambiguation scorer"
      exports: ["evaluators"]
    - path: "evals/datasets/salla-integration-v1.ts"
      provides: "Dataset for the Salla family (6 tools)"
      exports: ["items"]
    - path: "evals/salla-integration.ts"
      provides: "Langfuse experiment runner for the Salla family (includes the token-omission STATIC scorer)"
      contains: "experiment.run"
    - path: "evals/score-salla-integration.ts"
      provides: "Evaluators including the token-omission source-inspection scorer + 404-as-null documentation scorer"
      exports: ["evaluators"]
    - path: "evals/datasets/destructive-integrations-v1.ts"
      provides: "Dataset for the destructive-integrations family (2 tools)"
      exports: ["items"]
    - path: "evals/destructive-integrations.ts"
      provides: "Langfuse experiment runner for the destructive family (includes the confirm-gate-present STATIC scorer)"
      contains: "experiment.run"
    - path: "evals/score-destructive-integrations.ts"
      provides: "Evaluators including the confirm-gate-present + dry-run-present + tight-rate-limit scorers"
      exports: ["evaluators"]
  key_links:
    - from: "evals/integrations-shared.ts"
      to: "lib/tools/list-integration-connections.ts and 4 siblings"
      via: "import spec — description is mirrored, not copied"
      pattern: "spec.description"
    - from: "evals/salla-integration.ts"
      to: "evals/score-salla-integration.ts (token-omission source-inspection scorer)"
      via: "STATIC scorer reads lib/tools/get-salla-connection.ts file text via fs.readFile"
      pattern: "readFile"
    - from: "evals/destructive-integrations.ts"
      to: "evals/score-destructive-integrations.ts (confirm-gate-present STATIC scorer)"
      via: "STATIC scorer imports spec.inputSchema and asserts shape.confirm exists"
      pattern: "inputSchema.shape.confirm"
    - from: ".github/workflows/eval-gate.yml"
      to: "5 new eval jobs"
      via: "EVAL_GATE=1 bun run eval:* steps"
      pattern: "EVAL_GATE"
---

<objective>
Add Langfuse eval coverage for the 5 service-host sub-families introduced or extended by Phase 2: shared-integrations (5 tools), Shopify (6 tools), WooCommerce (6 tools), Salla (6 tools), destructive-integrations (2 tools). Each new family gets its own dataset + experiment runner + scorers + CI gate step.

Purpose: PROJECT.md "Every new service-host family gets at least one Langfuse eval before its tools count as shipped." Phase 2 introduces 25 tools across 5 logical sub-families (the source-doc groups them this way and each has its own description-quality regression surface). Five evals close all five surfaces in a single plan rather than one-eval-per-tool which would be ~25 evals.

Output: 5 new eval runners, 5 new datasets, 5 new score files, 5 new CI gate steps wired with EVAL_GATE=1, and 5 new package.json scripts.
</objective>

<execution_context>
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# Closest analog evals — copy structure exactly
@evals/get-account.ts
@evals/score-get-account.ts
@evals/datasets/get-account-v1.ts
@evals/lookup-google-place.ts
@evals/score-lookup-google-place.ts
@evals/datasets/lookup-google-place-v1.ts

# Existing tool-surface description-quality eval
@evals/tool-surface.ts

# Eval gate utility
@evals/gate.ts

# CI workflow (extend with 5 new jobs)
@.github/workflows/eval-gate.yml

# All 25 production tool specs (the evals import description + inputSchema directly)
@lib/tools/list-integration-connections.ts
@lib/tools/list-integration-order-reasons.ts
@lib/tools/repair-integration-orders.ts
@lib/tools/get-integration-order.ts
@lib/tools/confirm-ff-export.ts
@lib/tools/get-shopify-config.ts
@lib/tools/list-shopify-delivery-methods.ts
@lib/tools/list-shopify-locations.ts
@lib/tools/update-shopify-config.ts
@lib/tools/update-shopify-connection.ts
@lib/tools/setup-shopify-callback.ts
@lib/tools/list-woocommerce-connections.ts
@lib/tools/get-woocommerce-config.ts
@lib/tools/list-woocommerce-states.ts
@lib/tools/list-woocommerce-shipping-lines.ts
@lib/tools/setup-woocommerce-connection.ts
@lib/tools/upsert-woocommerce-config.ts
@lib/tools/install-salla.ts
@lib/tools/get-salla-connection.ts
@lib/tools/get-salla-platform-data.ts
@lib/tools/get-salla-config.ts
@lib/tools/update-salla-config.ts
@lib/tools/toggle-salla-fulfillment.ts
@lib/tools/delete-integration-source.ts
@lib/tools/delete-salla-connection.ts

@lib/middleware/destructive.ts

# Package.json (add 5 new eval:* scripts)
@package.json

<interfaces>
<!-- Langfuse experiment pattern (from evals/get-account.ts / lookup-google-place.ts):
       - Dataset = `items` array, each item shape `{ input: { request: string }, expected: { tool: string, args: Record<string, unknown> } }`.
       - Task function takes one item, calls Anthropic with the family's tools exposed, returns `{ tool: block.name, args: block.input }`.
       - `evaluators` is an array of named scorer functions returning `{ name, value }`.
       - `langfuse.experiment.run({ name, description, data, task, evaluators })` runs the suite.
       - `EVAL_DRY_RUN=1` → print and exit.
       - `EVAL_GATE=1` → invokes gate(result, [{ scoreName, min }]) at the end.

     Spec import + drift-proofing:
       - Import each production `spec` directly; build the Anthropic `tools` payload from spec.name/description/inputSchema.
       - This means description text changes in lib/tools/*.ts flow into the eval automatically.

     STATIC scorers (do not depend on LLM output; return the same value for every item):
       - description-quality: assert substrings in spec.description.
       - source-inspection: readFile() the production source file, assert/disprove substrings (used for token-omission on get_salla_connection).
       - schema-shape: import spec.inputSchema and assert the presence/absence of specific fields (used for confirm-gate on destructive tools). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared-integrations + Shopify + WooCommerce evals (3 families)</name>
  <files>evals/datasets/integrations-shared-v1.ts, evals/score-integrations-shared.ts, evals/integrations-shared.ts, evals/datasets/shopify-integration-v1.ts, evals/score-shopify-integration.ts, evals/shopify-integration.ts, evals/datasets/woocommerce-integration-v1.ts, evals/score-woocommerce-integration.ts, evals/woocommerce-integration.ts, package.json</files>
  <read_first>
    - evals/get-account.ts (eval-runner skeleton — copy verbatim for each new family)
    - evals/score-get-account.ts (scorer shape: tool-name-match, required-fields-present, args-overlap, description-quality)
    - evals/datasets/get-account-v1.ts (dataset shape with TODAY anchor)
    - evals/gate.ts (gate function + EVAL_GATE=1 contract)
    - lib/tools/list-integration-connections.ts, repair-integration-orders.ts (production specs)
    - lib/tools/get-shopify-config.ts, update-shopify-config.ts, setup-shopify-callback.ts (Shopify specs)
    - lib/tools/list-woocommerce-states.ts, upsert-woocommerce-config.ts (WooCommerce specs)
    - package.json (existing eval:* scripts)
  </read_first>
  <action>
    For EACH of the three families (integrations-shared, shopify-integration, woocommerce-integration) produce: 1 dataset file, 1 score file, 1 experiment runner. Use evals/get-account.ts as the canonical structure (import production specs, build Anthropic tools[] from spec.name + spec.description + zod-to-json-schema(spec.inputSchema), task returns `{ tool, args }`, evaluators run, EVAL_GATE block at the end).

    ---

    SHARED-INTEGRATIONS (5 tools: list_integration_connections, list_integration_order_reasons, repair_integration_orders, get_integration_order, confirm_ff_export):

    - evals/datasets/integrations-shared-v1.ts → 6-8 hand-authored merchant prompts. Cover at least: "show me all my connected stores across shopify woocommerce and salla" → list_integration_connections (no args required); "what integration orders failed in the last 24 hours on my shopify store?" → list_integration_order_reasons with sales_channel: "shopify", status: "failed", a start_date roughly TODAY-1 (use the dataset's TODAY anchor), end_date TODAY, user_id, limit, offset; "retry failed integration orders with these 3 ids: a, b, c on acme shopify shop https://acme.myshopify.com" → repair_integration_orders with the appropriate args; "look up integration order with UUID xyz-123" → get_integration_order with order_uuid: "xyz-123"; "ack the fulfilment export for order_uuid abc-456" → confirm_ff_export with order_uuid: "abc-456".

    - evals/score-integrations-shared.ts → exports `evaluators` (4 scorers): tool-name-match, required-fields-present (with per-tool rules — repair_integration_orders requires ids+shop_name+site_url+source+user_id+start_date+end_date; list_integration_order_reasons requires all 7 query params; get_integration_order requires order_uuid; confirm_ff_export requires order_uuid; list_integration_connections has no required fields), args-overlap, description-quality (STATIC — asserts each of the 5 production descriptions contains the endpoint path AND "401" AND the canonical companion-tool reference where applicable — e.g. get_integration_order description mentions repair_integration_orders).

    - evals/integrations-shared.ts → copy evals/get-account.ts; import the 5 specs; build the tools[] array; task uses tool_choice `{ type: "any" }`; experiment.run with name `phase2-integrations-shared-v1 (${MODEL})`; EVAL_GATE block: `gate(result, [{ scoreName: "args-overlap", min: 0.7 }, { scoreName: "description-quality", min: 1.0 }])` (args-overlap min 0.7 here — repair_integration_orders has 7 required args so a small LLM miss on one drops the score; 0.7 keeps the gate green while still catching regressions).

    ---

    SHOPIFY-INTEGRATION (6 tools):

    - evals/datasets/shopify-integration-v1.ts → 6-8 prompts: "show me the saved config for my shopify store acme-store" → get_shopify_config; "list the live shipping methods for acme-store" → list_shopify_delivery_methods; "list the ship-from locations for acme-store" → list_shopify_locations; "update acme-store config: turn off auto_mark_as_rfc" → update_shopify_config with shop_name: "acme-store", auto_mark_as_rfc: false; "complete the shopify oauth callback for acme-store with code XYZ and is_fulfillment true" → setup_shopify_callback with shop_name: "acme-store", code: "XYZ", is_fulfillment: true; "update the shopify connection meta for acme-store using token TOK and code XYZ as fulfillment" → update_shopify_connection.

    - evals/score-shopify-integration.ts → 5 scorers (tool-name-match, required-fields-present, args-overlap, description-quality, sensitive-and-single-use-language). The 5th is STATIC and checks: spec.description on update_shopify_connection contains "sensitive" or "secret" (T-02-12); spec.description on setup_shopify_callback contains "single-use" (T-02-13). Score = fraction of assertions passing.

    - evals/shopify-integration.ts → copy evals/get-account.ts. EVAL_GATE: `gate(result, [{ scoreName: "args-overlap", min: 0.75 }, { scoreName: "description-quality", min: 1.0 }, { scoreName: "sensitive-and-single-use-language", min: 1.0 }])`.

    ---

    WOOCOMMERCE-INTEGRATION (6 tools):

    - evals/datasets/woocommerce-integration-v1.ts → 6-8 prompts: "list my woocommerce connections" → list_woocommerce_connections; "what quiqup order states are available for my woocommerce mapping?" → list_woocommerce_states; "what shipping lines does my acme.example.com site have?" → list_woocommerce_shipping_lines with site_url: "https://acme.example.com"; "set up a woocommerce connection for acme-store at https://acme.example.com using consumer secret SECRET, fulfillment yes" → setup_woocommerce_connection; "save woocommerce config for https://acme.example.com: only ship to US and AE, sync_products: true" → upsert_woocommerce_config; "show me the woocommerce config for site acme-store" → get_woocommerce_config with site_name: "acme-store".

    - evals/score-woocommerce-integration.ts → 5 scorers (tool-name-match, required-fields-present, args-overlap, description-quality, quiqup-vs-woocommerce-state-disambiguation). The 5th is STATIC and reads spec.description on list_woocommerce_states; asserts it contains BOTH "quiqup" (case-insensitive) AND "woocommerce" (case-insensitive). description-quality scorer also asserts upsert_woocommerce_config description mentions list_woocommerce_states + list_woocommerce_shipping_lines.

    - evals/woocommerce-integration.ts → copy structure. EVAL_GATE: `gate(result, [{ scoreName: "args-overlap", min: 0.75 }, { scoreName: "description-quality", min: 1.0 }, { scoreName: "quiqup-vs-woocommerce-state-disambiguation", min: 1.0 }])`.

    ---

    Step Z — append to package.json scripts (do NOT reorder existing entries):
      "eval:integrations-shared": "bun run evals/integrations-shared.ts",
      "eval:shopify-integration": "bun run evals/shopify-integration.ts",
      "eval:woocommerce-integration": "bun run evals/woocommerce-integration.ts"
  </action>
  <verify>
    <automated>EVAL_DRY_RUN=1 bun run eval:integrations-shared && EVAL_DRY_RUN=1 bun run eval:shopify-integration && EVAL_DRY_RUN=1 bun run eval:woocommerce-integration && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 9 new files exist (3 datasets, 3 score files, 3 experiment runners).
    - Each dataset file: `grep -c "^  {" evals/datasets/integrations-shared-v1.ts` >= 6, same for shopify and woocommerce datasets.
    - Each score file: `grep -c "name: \"" evals/score-integrations-shared.ts` >= 4, `grep -c "name: \"" evals/score-shopify-integration.ts` >= 5, `grep -c "name: \"" evals/score-woocommerce-integration.ts` >= 5.
    - Each experiment runner: `grep -c "spec.description" evals/integrations-shared.ts evals/shopify-integration.ts evals/woocommerce-integration.ts` >= 3 (no inline copies).
    - Each runner: `grep -c "EVAL_GATE" evals/integrations-shared.ts evals/shopify-integration.ts evals/woocommerce-integration.ts` >= 3.
    - package.json: `jq -r '.scripts["eval:integrations-shared"]' package.json` returns non-null; same for the other 2.
    - All three `EVAL_DRY_RUN=1 bun run eval:*` invocations exit 0 and print the dataset item count.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Three new family evals (shared-integrations, Shopify, WooCommerce) are runnable as dry-runs, sourced from production spec.description (drift-proofed), and ready for CI wiring in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the Salla + destructive-integrations evals (2 families) with STATIC source-inspection scorers</name>
  <files>evals/datasets/salla-integration-v1.ts, evals/score-salla-integration.ts, evals/salla-integration.ts, evals/datasets/destructive-integrations-v1.ts, evals/score-destructive-integrations.ts, evals/destructive-integrations.ts, package.json</files>
  <read_first>
    - evals/lookup-google-place.ts (analog with a STATIC auth-isolation source-inspection scorer — copy that pattern for token-omission)
    - evals/score-lookup-google-place.ts (5-scorer file including auth-isolation that readFile()s a production module)
    - evals/integrations-shared.ts (Task 1 output — same skeleton)
    - lib/tools/get-salla-connection.ts and lib/tools/get-salla-config.ts (the special-case production tools)
    - lib/tools/delete-integration-source.ts and lib/tools/delete-salla-connection.ts (the destructive tools)
    - lib/middleware/destructive.ts (the canonical helper; import destructiveConfirmField for the confirm-gate scorer's shape check)
  </read_first>
  <action>
    Two new family evals.

    SALLA-INTEGRATION (6 tools: install_salla, get_salla_connection, get_salla_platform_data, get_salla_config, update_salla_config, toggle_salla_fulfillment):

    - evals/datasets/salla-integration-v1.ts → 6-8 prompts: "get me the salla install/oauth URL" → install_salla; "read the salla connection with id c-abc" → get_salla_connection with id: "c-abc"; "show me salla platform data (shipping methods + locations) for connection c-abc" → get_salla_platform_data with connection_id: "c-abc"; "is there a salla config saved for connection c-abc?" → get_salla_config; "save salla config for c-abc: awb_trigger ready_for_collection, sync_products true" → update_salla_config; "turn on fulfillment for salla connection c-abc" → toggle_salla_fulfillment with id: "c-abc", is_fulfillment: true.

    - evals/score-salla-integration.ts → 6 scorers (tool-name-match, required-fields-present, args-overlap, description-quality, token-omission, four-oh-four-as-null). 
        - description-quality (STATIC) asserts: spec.description on update_salla_config contains "list_service_kinds" (cross-phase reference, T-02-33); spec.description on get_salla_config contains "no config" or "not yet" (404-as-null semantic, T-02-30); spec.description on get_salla_connection contains "token" (omission contract, T-02-29).
        - token-omission (STATIC, the highest-value scorer for this family): import "node:fs/promises", readFile("lib/tools/get-salla-connection.ts", "utf8"), assert the handler returns the unwrapped + token-stripped shape. Concretely: assert the source contains BOTH "{ token: _token, ...connectionSafe }" (or equivalent destructure that strips the token) AND "connectionSafe" appears in the return path. Score 1.0 if both substrings present; 0.0 otherwise. This is the eval-layer mirror of T-02-29 — a maintainer cannot land a regression that re-leaks the token without simultaneously editing or deleting this scorer (and either change shows up in PR review).
        - four-oh-four-as-null (STATIC): readFile("lib/tools/get-salla-config.ts"), assert source contains "status === 404" AND "config: null" (or "\"config\": null"). Locks in T-02-30 at the eval layer.

    - evals/salla-integration.ts → copy evals/lookup-google-place.ts structure. EVAL_GATE: `gate(result, [{ scoreName: "args-overlap", min: 0.75 }, { scoreName: "description-quality", min: 1.0 }, { scoreName: "token-omission", min: 1.0 }, { scoreName: "four-oh-four-as-null", min: 1.0 }])`.

    ---

    DESTRUCTIVE-INTEGRATIONS (2 tools: delete_integration_source, delete_salla_connection):

    - evals/datasets/destructive-integrations-v1.ts → 4-5 prompts mixing positive and negative paths. Examples: "delete my shopify integration source for shop acme-store, confirm true" → delete_integration_source with source: "shopify", shop_name: "acme-store", confirm: true; "preview what would happen if I deleted the salla connection c-abc (dry-run)" → delete_salla_connection with id: "c-abc", confirm: true, dry_run: true; "remove the woocommerce source for store acme-store" → delete_integration_source — IMPORTANT: in this prompt the LLM SHOULD include confirm: true in its args even though the user prompt does not say "confirm" explicitly, because the tool description tells it confirm is required. So the EXPECTED args include confirm: true. This tests that the description's BL-01-canonical confirm warning is strong enough to elicit the field from the LLM. ALSO include one prompt where the user explicitly says "do not actually delete, just preview" — expected args include confirm: true (still required to dry-run) AND dry_run: true.

    - evals/score-destructive-integrations.ts → 5 scorers:
        1. tool-name-match.
        2. required-fields-present (delete_integration_source requires source + shop_name; delete_salla_connection requires id).
        3. args-overlap.
        4. confirm-elicited (the args-overlap variant for the confirm field — score 1.0 if output.args.confirm === expected.args.confirm, 0.0 otherwise; reports separately so a regression in the description's confirm-elicitation language is visible at the trace level).
        5. confirm-gate-present (STATIC): import the two specs and assert spec.inputSchema.shape.confirm is defined on BOTH tools; assert it's the SAME instance / wraps the same Zod type as destructiveConfirmField from lib/middleware/destructive.ts. A maintainer cannot silently remove the gate without this scorer flipping to 0. Score = 1.0 only if both tools have the field AND both expose dry_run via destructiveDryRunField.

    - evals/destructive-integrations.ts → copy evals/lookup-google-place.ts structure. EVAL_GATE: `gate(result, [{ scoreName: "args-overlap", min: 0.7 }, { scoreName: "confirm-elicited", min: 0.75 }, { scoreName: "confirm-gate-present", min: 1.0 }])`.

    Step Z — append to package.json scripts:
      "eval:salla-integration": "bun run evals/salla-integration.ts",
      "eval:destructive-integrations": "bun run evals/destructive-integrations.ts"
  </action>
  <verify>
    <automated>EVAL_DRY_RUN=1 bun run eval:salla-integration && EVAL_DRY_RUN=1 bun run eval:destructive-integrations && pnpm tsc --noEmit && grep -c "readFile" evals/score-salla-integration.ts && grep -c "destructiveConfirmField" evals/score-destructive-integrations.ts</automated>
  </verify>
  <acceptance_criteria>
    - 6 new files exist (2 datasets, 2 score files, 2 experiment runners).
    - evals/datasets/salla-integration-v1.ts has >= 6 items; evals/datasets/destructive-integrations-v1.ts has >= 4 items.
    - evals/score-salla-integration.ts: `grep -c "name: \"" evals/score-salla-integration.ts` >= 6.
    - evals/score-salla-integration.ts: `grep -c "readFile" evals/score-salla-integration.ts` >= 2 (token-omission AND four-oh-four-as-null both read source files).
    - evals/score-destructive-integrations.ts: `grep -c "name: \"" evals/score-destructive-integrations.ts` >= 5.
    - evals/score-destructive-integrations.ts: `grep -c "destructiveConfirmField" evals/score-destructive-integrations.ts` >= 1 (the confirm-gate-present scorer imports the canonical field).
    - evals/score-destructive-integrations.ts: `grep -c "inputSchema.shape.confirm" evals/score-destructive-integrations.ts` >= 1.
    - evals/salla-integration.ts: `grep -c "EVAL_GATE" evals/salla-integration.ts` >= 1, `grep -c "token-omission" evals/salla-integration.ts` >= 1.
    - evals/destructive-integrations.ts: `grep -c "confirm-gate-present" evals/destructive-integrations.ts` >= 1.
    - package.json: `jq -r '.scripts["eval:salla-integration"]' package.json` and `jq -r '.scripts["eval:destructive-integrations"]' package.json` both return non-null.
    - Both `EVAL_DRY_RUN=1 bun run eval:*` invocations exit 0.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Salla family has its first eval with TWO source-inspection STATIC scorers (token-omission + 404-as-null) that lock the canonical Salla behaviour invariants at the CI layer. Destructive-integrations family has the canonical confirm-gate-present scorer that imports destructiveConfirmField directly so removing or renaming the gate is structurally impossible without simultaneously breaking the eval. "Every new service-host family has at least one Langfuse eval" extends to Phase 2's 5 sub-families.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire all 5 new evals into the CI gate</name>
  <files>.github/workflows/eval-gate.yml</files>
  <read_first>
    - .github/workflows/eval-gate.yml (current state — has tool-surface + get-account + lookup-google-place jobs)
    - evals/integrations-shared.ts, shopify-integration.ts, woocommerce-integration.ts, salla-integration.ts, destructive-integrations.ts (Task 1 + 2 output)
  </read_first>
  <action>
    Edit .github/workflows/eval-gate.yml. Add 5 new jobs MIRRORING the existing get-account / lookup-google-place job structure (separate `job` per eval, each with its own bun setup + install + EVAL_GATE=1 step). Job names follow the existing convention `phase2-<family>`:

      - integrations-shared:
          name: "Eval gate — phase2-integrations-shared (5 tools)"
          step: `bun run eval:integrations-shared`
          gates: args-overlap >= 0.7, description-quality >= 1.0.
      - shopify-integration:
          name: "Eval gate — phase2-shopify-integration (6 tools)"
          step: `bun run eval:shopify-integration`
          gates: args-overlap >= 0.75, description-quality >= 1.0, sensitive-and-single-use-language >= 1.0.
      - woocommerce-integration:
          name: "Eval gate — phase2-woocommerce-integration (6 tools)"
          step: `bun run eval:woocommerce-integration`
          gates: args-overlap >= 0.75, description-quality >= 1.0, quiqup-vs-woocommerce-state-disambiguation >= 1.0.
      - salla-integration:
          name: "Eval gate — phase2-salla-integration (6 tools; token-omission + 404-as-null locked)"
          step: `bun run eval:salla-integration`
          gates: args-overlap >= 0.75, description-quality >= 1.0, token-omission >= 1.0, four-oh-four-as-null >= 1.0.
      - destructive-integrations:
          name: "Eval gate — phase2-destructive-integrations (2 tools; confirm-gate locked)"
          step: `bun run eval:destructive-integrations`
          gates: args-overlap >= 0.7, confirm-elicited >= 0.75, confirm-gate-present >= 1.0.

    Each new job MUST have the SAME secrets+env block as the existing `get-account` job (LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_BASE_URL, ANTHROPIC_API_KEY, EVAL_GATE=1). Reuse the same `oven-sh/setup-bun@v2` + `bun install --frozen-lockfile` setup steps verbatim. The existing tool-surface, get-account, and lookup-google-place jobs MUST remain unchanged.
  </action>
  <verify>
    <automated>test -f .github/workflows/eval-gate.yml && grep -c "eval:integrations-shared\|eval:shopify-integration\|eval:woocommerce-integration\|eval:salla-integration\|eval:destructive-integrations" .github/workflows/eval-gate.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eval:integrations-shared" .github/workflows/eval-gate.yml` >= 1.
    - `grep -c "eval:shopify-integration" .github/workflows/eval-gate.yml` >= 1.
    - `grep -c "eval:woocommerce-integration" .github/workflows/eval-gate.yml` >= 1.
    - `grep -c "eval:salla-integration" .github/workflows/eval-gate.yml` >= 1.
    - `grep -c "eval:destructive-integrations" .github/workflows/eval-gate.yml` >= 1.
    - `grep -c "EVAL_GATE: \"1\"" .github/workflows/eval-gate.yml` increased by exactly 5 vs git HEAD.
    - Existing jobs remain: `grep -c "eval:tool-surface\|eval:get-account\|eval:lookup-google-place" .github/workflows/eval-gate.yml` >= 3.
    - YAML is parseable: `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/eval-gate.yml','utf8'))"` exits 0 (if js-yaml is not in node_modules, accept any successful YAML lint — e.g. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/eval-gate.yml'))"`).
  </acceptance_criteria>
  <done>
    CI runs all 5 new evals on every PR (plus the existing 3 jobs), failing the build on regression. Phase 2's "every new family gets at least one Langfuse eval" invariant is satisfied at the CI gate layer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → Anthropic API | EVAL runs send dataset prompts to Anthropic; no production data, no user PII — only hand-authored merchant prompts in the dataset files. |
| CI runner → Langfuse | Trace + score upload via LANGFUSE_PUBLIC_KEY/SECRET_KEY (CI secrets). |
| Eval runner → nothing else | Offline: does NOT hit Quiqup. The whole eval validates description-quality + args-overlap + source-inspection scores via the LLM + readFile; no live upstream traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-47 | Information Disclosure | Dataset prompts leaking PII into Langfuse | mitigate | All 5 datasets are hand-authored merchant prompts. Names like "acme-store", "https://acme.example.com", connection ids like "c-abc", tokens like "SECRET" are obvious placeholders — no real partner data. |
| T-02-48 | Tampering | Eval skipping in CI (silent regression) | mitigate | EVAL_GATE=1 is set explicitly on all 5 new jobs. Without it, the gate is no-op. Task 3 grep-asserts the new EVAL_GATE entries are present. |
| T-02-49 | Information Disclosure | Drift between production tool description and eval-time description | mitigate | All 5 evals import `spec.description` from the production module rather than copy-pasting. Drift is structurally impossible. Mirrored from plan 01-04 T-01-26. |
| T-02-50 | Elevation of Privilege | description-quality scorer false positives | accept | The static description-quality scorers assert substring presence (e.g. "single-use" for setup_shopify_callback). A maintainer could trivially game the score by sprinkling the substrings without writing a real description. Accepted because args-overlap is the real quality signal; description-quality is the cheap lockdown for known disambiguation language. Mirrored from plan 01-04 T-01-27. |
| T-02-51 | Spoofing | Eval bypass via the token-omission source-string scorer | mitigate | The token-omission scorer literally readFile()'s lib/tools/get-salla-connection.ts and substring-checks for the destructure pattern. A maintainer cannot land a regression on the Salla token-omission surface without simultaneously deleting or relocating that scorer — either change is visible in PR review. Mirrored from plan 01-04 T-01-28. |
| T-02-52 | Spoofing | Eval bypass via the confirm-gate-present scorer | mitigate | The confirm-gate-present scorer imports `destructiveConfirmField` from lib/middleware/destructive.ts AND asserts the value identity on `spec.inputSchema.shape.confirm` for both destructive tools. Renaming the helper or detaching the spec field both trip the scorer. The scorer also asserts dry_run is wired the same way (destructiveDryRunField), preventing partial removal. |
| T-02-53 | Tampering | confirm-elicited scorer false negative | accept | The confirm-elicited scorer scores 0 if the LLM does not include `confirm: true` in its tool args. This depends on description-driven elicitation. If the model is too cautious and ALSO produces "user must confirm" as a text response WITHOUT a tool call, the scorer flags zero. min: 0.75 is calibrated to allow up to one such mis-elicitation across the 4-5 dataset items without failing the gate. If real-world false-negative rate exceeds that, tighten the description in delete_integration_source / delete_salla_connection and re-baseline. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required if zod-to-json-schema (or the inline fallback from plan 01-04 Task 1 Step C) is already a dep. If absent, follow the same inline fallback approach. Plan is zero-install. |
</threat_model>

<verification>
- `EVAL_DRY_RUN=1 bun run eval:integrations-shared` exits 0.
- `EVAL_DRY_RUN=1 bun run eval:shopify-integration` exits 0.
- `EVAL_DRY_RUN=1 bun run eval:woocommerce-integration` exits 0.
- `EVAL_DRY_RUN=1 bun run eval:salla-integration` exits 0.
- `EVAL_DRY_RUN=1 bun run eval:destructive-integrations` exits 0.
- `pnpm tsc --noEmit` exits 0.
- `pnpm test` exits 0.
- .github/workflows/eval-gate.yml gates all 5 new evals with EVAL_GATE=1.
- `EVAL_GATE=1 bun run eval:tool-surface` continues to exit 0 (existing snapshot unaffected).
</verification>

<success_criteria>
1. 5 new Langfuse evals exist (integrations-shared, shopify-integration, woocommerce-integration, salla-integration, destructive-integrations) each with its own dataset + scorers + runner.
2. All 5 eval scripts run as offline dry-runs.
3. All 5 are wired into the CI gate with EVAL_GATE=1.
4. The Salla family eval has a STATIC token-omission scorer that readFile()'s lib/tools/get-salla-connection.ts and asserts the destructure-and-discard pattern.
5. The destructive family eval has a STATIC confirm-gate-present scorer that imports destructiveConfirmField from lib/middleware/destructive.ts and asserts spec.inputSchema.shape.confirm is bound on both delete tools.
6. The Salla family eval has a STATIC 404-as-null scorer that readFile()'s lib/tools/get-salla-config.ts and asserts the 404 special-case is wired.
7. Each eval imports `spec.description` directly from the production modules (no copy-drift).
8. PROJECT.md "Every new service-host family gets at least one Langfuse eval" invariant is satisfied for Phase 2's 5 sub-families.
</success_criteria>

<output>
Create `.planning/phases/02-integrations/02-06-SUMMARY.md` when done.

In the SUMMARY, record:
- 5 new evals = full Phase 2 family coverage (shared + 3 storefronts + destructives).
- 3 STATIC source-inspection scorers established for Phase 2 (token-omission, four-oh-four-as-null, confirm-gate-present) — these are the canonical "lock production invariants at the CI layer" pattern, mirroring plan 01-04 Task 2 Step B (auth-isolation). Future phases with similar must-haves SHOULD reuse this pattern.
- Phase 2 ships with 5/5 family eval coverage. Phase 12 (Eval Coverage Pass) will continue to add evals for Phase 3-11 families.
</output>
