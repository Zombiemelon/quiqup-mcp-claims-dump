---
phase: 03-orders-read-path
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - lib/clients/ex-core.ts
  - lib/clients/orders-core-rest.ts
  - lib/tools/download-orders-export.ts
  - lib/tools/upload-order-document.ts
  - tests/clients/ex-core.test.ts
  - tests/clients/orders-core-rest.test.ts
  - tests/tools/orders-export-and-upload.test.ts
  - app/[transport]/route.ts
  - evals/snapshots/tool-surface.json
autonomous: true
requirements:
  - ORDL-07
  - ORDS-08
must_haves:
  truths:
    - "An Ex-core CSV export client exists at lib/clients/ex-core.ts with explicit env wiring (`EX_API_BASE_URL` / `EX_API_STAGING_BASE_URL`), reuses the Clerk → Quiqup Bearer-JWT bridge, and returns CSV responses as the canonical base64 contract `{ contentType, base64, filenameHint }` (Phase 3 success criteria 4)."
    - "An Orders Core REST client exists at lib/clients/orders-core-rest.ts with explicit env wiring (`ORDERS_API_BASE_URL` / `ORDERS_API_STAGING_BASE_URL`, falling back to `ORDERS_API_GRAPH_URL` minus `/graph` per the source-doc fallback chain), reuses the Clerk → Quiqup Bearer-JWT bridge, and handles `multipart/form-data` POSTs via the standard `FormData` API."
    - "Agent can download a CSV export of orders via download_orders_export (ORDL-07 → GET /orders/download). The CSV is returned as base64 with `contentType: \"text/csv\"` and a filename hint of `\"orders-export-{from}-to-{to}.csv\"`."
    - "Agent can upload a document to an order via upload_order_document (ORDS-08 → POST /orders-by-client-id/{clientOrderID}/documents). Input takes `file_base64 + filename + document_type` (the `proof_of_delivery` literal observed in the FE, but the field is left as a string to accommodate future doc types); the server-side handler builds the multipart envelope. Returns the upstream's document reference."
    - "`upload_order_document` is a WRITE tool — it MUST carry the canonical `guardrails: { rateLimit, idempotency, audit: true }` block per the BL-01 lesson (audit visibility + idempotency dedupe + rate-limit cap)."
    - "`upload_order_document` MUST server-bind any identity field — the input schema deliberately has no `user_id` / `actor_id` / `actor_email` field; the auth identity is derived exclusively from `auth.userId` (BL-04 lesson — no cross-tenant smuggling)."
    - "Date filters on download_orders_export use `z.string().regex(/^\\d{4}-\\d{2}-\\d{2}$/)` (the source-doc explicitly states `yyyy-mm-dd` on this endpoint, NOT full ISO-8601 — WR-02 lesson, generalized: enforce the format the upstream actually expects)."
    - "Tests run `delete process.env.EX_API_BASE_URL` / `EX_API_STAGING_BASE_URL` / `ORDERS_API_BASE_URL` / `ORDERS_API_STAGING_BASE_URL` / `ORDERS_API_GRAPH_URL` / `QUIQUP_ORDERS_GRAPH_URL` (the last two cover the fallback chain) in beforeEach per WR-05."
    - "Tool output schemas/handlers MUST NOT echo any auth-bearing field (token, bearer, jwt, code, secret) back to the agent — belt-and-braces over the ALWAYS_REDACT_KEYS audit-log redactor (BL-02 / BL-03 lesson generalized)."
  artifacts:
    - path: "lib/clients/ex-core.ts"
      provides: "Ex-core CSV-export client (ex-api.quiqup.com — reuses Bearer JWT bridge; non-JSON responses returned as base64 envelope)"
      exports: ["ExCoreClient", "ExCoreError", "getExCoreBaseUrl", "EX_CORE_BASE_URLS"]
    - path: "lib/clients/orders-core-rest.ts"
      provides: "Orders Core REST client (host derived from ORDERS_API_GRAPH_URL minus /graph; reuses Bearer JWT bridge; multipart POST support)"
      exports: ["OrdersCoreRestClient", "getOrdersRestBaseUrl", "ORDERS_REST_BASE_URLS"]
    - path: "lib/tools/download-orders-export.ts"
      provides: "ORDL-07 tool spec — GET /orders/download (Ex-core, CSV) — returns the base64 envelope"
      exports: ["spec"]
    - path: "lib/tools/upload-order-document.ts"
      provides: "ORDS-08 tool spec — POST /orders-by-client-id/{clientOrderID}/documents (Orders Core REST, multipart)"
      exports: ["spec"]
    - path: "tests/clients/ex-core.test.ts"
      provides: "MSW client tests — covers Bearer header, CSV base64 envelope, env override, error path"
      contains: "describe(\"ExCoreClient\""
    - path: "tests/clients/orders-core-rest.test.ts"
      provides: "MSW client tests — covers multipart envelope shape, the ORDERS_API_GRAPH_URL minus /graph fallback chain, error path"
      contains: "describe(\"OrdersCoreRestClient\""
    - path: "tests/tools/orders-export-and-upload.test.ts"
      provides: "Tool-level Vitest suite (auth + happy path + 401 + guardrails)"
      contains: "describe(\"download_orders_export\""
  key_links:
    - from: "lib/clients/ex-core.ts"
      to: "lib/clients/quiqup-lastmile.ts (QuiqupHttpError pattern)"
      via: "ExCoreError mirrors QuiqupHttpError shape — same registerTool-wrapper-compatible status/body fields"
      pattern: "status.*body"
    - from: "lib/tools/download-orders-export.ts"
      to: "lib/clients/ex-core.ts (ExCoreClient + base64 envelope)"
      via: "Reads CSV stream, returns `{ contentType, base64, filenameHint }` per the binary-response contract"
      pattern: "base64"
    - from: "lib/tools/upload-order-document.ts"
      to: "lib/clients/orders-core-rest.ts (OrdersCoreRestClient)"
      via: "Server-side FormData multipart build; tool MUST NOT trust caller-supplied user_id"
      pattern: "FormData"
    - from: "lib/tools/upload-order-document.ts"
      to: "lib/middleware/audit.ts + lib/middleware/rate-limit.ts (via registerTool guardrails)"
      via: "BL-01 canonical: write tools carry rateLimit + idempotency + audit:true"
      pattern: "guardrails:"
---

<objective>
Stand up the last two new service clients required by Phase 3:
  (a) Ex-core (legacy Ruby `ex-core`) CSV export client — bound to a NEW env-var family (`EX_API_BASE_URL` / `EX_API_STAGING_BASE_URL`), reuses the Clerk → Quiqup Bearer-JWT bridge (Ex-core sits behind the same gateway as the other Quiqup services per source-doc §1), and returns CSV/binary responses as the canonical base64 envelope `{ contentType, base64, filenameHint }`.
  (b) Orders Core REST client — bound to env vars `ORDERS_API_BASE_URL` / `ORDERS_API_STAGING_BASE_URL` with a documented fallback chain to `ORDERS_API_GRAPH_URL` minus `/graph` (matches the FE behaviour at source-doc §1 line 21). Supports the multipart/form-data POSTs that ORDS-08 needs.

Ship the two anchor tools:
  - `download_orders_export` (ORDL-07) — Phase 3 success criteria 4: "Agent can download a CSV export of orders … CSV returned as base64 per the binary-response contract."
  - `upload_order_document` (ORDS-08) — Phase 3 success criteria 5: "Agent can upload a document to an order (multipart against Orders Core REST) and receive the resulting document reference."

Purpose: Close Phase 3 — these two endpoints span the read AND write boundary (an export is read-only; an upload is a write) but are grouped together because they both introduce a new service client with binary-payload semantics (one outbound base64, one inbound base64). Treating them as one wave keeps the new-client work coherent and consolidates the binary-response contract used by Phase 5 (PDF labels), Phase 7 (inventory CSV), and Phase 10 (Zoho PDFs) downstream.

Output: 2 new client modules, 2 new tool spec files, 3 new test files (2 client + 1 tool), `app/[transport]/route.ts` registering both specs, and the tool-surface snapshot bump.
</objective>

<execution_context>
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Source-of-truth endpoint reference — §5 line 355 (POST /orders-by-client-id/{clientOrderID}/documents),
# §19 H lines 4671-4675 (multipart form fields), §19 H lines 4716-4721 (orders/download CSV)
@docs/quiqup-api-full-frontend-extract.md

# Service host map (lines 14-30 + 35-46) — Ex-core + Orders Core REST URLs and env-var family names
# Already in the source-doc reference above.

# Canonical clients we mirror structurally
@lib/clients/quiqup-lastmile.ts
@lib/clients/quiqup-fulfilment.ts
@lib/clients/quiqup-env.ts

# Auth bridge
@lib/quiqup.ts

# Tool wrapper
@lib/tools/register.ts

# Canonical write-tool pattern (post-BL-01 fix) — guardrails block shape
@lib/tools/update-account.ts
@lib/tools/repair-integration-orders.ts

# Canonical multipart/base64-input pattern (existing FE-side)
@lib/tools/bulk-validate-products.ts

# Canonical binary-output pattern (existing — base64 envelope)
@lib/clients/quiqup-lastmile.ts

# MCP server registration list + snapshot
@app/[transport]/route.ts
@evals/snapshots/tool-surface.json

# Test scaffolds — WR-05 env cleanup + write-tool body inspection
@tests/tools/auth-account-reads.test.ts
@tests/tools/integrations-shared.test.ts

<interfaces>
<!-- ToolSpec contract — same as prior plans.

     NEW helpers this plan introduces:

     lib/clients/ex-core.ts:
       EX_CORE_BASE_URLS = {
         production: "https://ex-api.quiqup.com",
         staging:    "https://ex-api.staging.quiqup.com",
       }
       getExCoreBaseUrl(env: QuiqupEnvironment): string
         - staging: process.env.EX_API_STAGING_BASE_URL ?? EX_CORE_BASE_URLS.staging
         - production: process.env.EX_API_BASE_URL ?? EX_CORE_BASE_URLS.production
       ExCoreError: class (status + body)
       ExCoreClient: class
         constructor({ jwt, baseUrl?, environment? })
         async request(method, path, init?: { query? }): Promise<unknown>
         - Authorization: Bearer <jwt>, Accept: */*  (CSV is text/csv, not application/json)
         - On non-2xx: throw new ExCoreError(status, body)
         - On 200: inspect Content-Type. If application/json → parse JSON. Otherwise → return base64 envelope `{ contentType, base64 }` (the tool layer adds filenameHint).

     lib/clients/orders-core-rest.ts:
       ORDERS_REST_BASE_URLS = {
         production: "https://orders-api.quiqup.com",
         staging:    "https://orders-api.staging.quiqup.com",
       }
       getOrdersRestBaseUrl(env: QuiqupEnvironment): string
         FALLBACK CHAIN (matches FE source-doc §1 line 21):
           1. staging mode: process.env.ORDERS_API_STAGING_BASE_URL → if set, return as-is
                            else process.env.QUIQUP_ORDERS_GRAPH_STAGING_URL with `/graph` suffix stripped → if set, return that
                            else ORDERS_REST_BASE_URLS.staging
           2. production mode: process.env.ORDERS_API_BASE_URL → if set, return as-is
                               else process.env.QUIQUP_ORDERS_GRAPH_URL with `/graph` suffix stripped → if set, return that
                               else ORDERS_REST_BASE_URLS.production
         Rationale (record in header): the FE reuses the Relay GraphQL host minus `/graph` as the Orders Core REST host when no separate REST env var is set — preserving that fallback means a single env var override (QUIQUP_ORDERS_GRAPH_URL) can redirect BOTH 03-01's GraphQL client AND this REST client in dev environments.
       OrdersCoreRestClient: class
         constructor({ jwt, baseUrl?, environment? })
         async requestMultipart(method, path, formData: FormData): Promise<unknown>
           - method usually "POST"
           - headers: Authorization: Bearer <jwt>, Accept: application/json
                     (DO NOT set Content-Type — fetch() sets multipart/form-data; boundary= automatically when passing a FormData body; setting it manually clobbers the boundary)
           - On non-2xx: throw new QuiqupHttpError(status, body)  (reuses existing error type — Orders Core IS a Quiqup-prefixed service so reusing the type is correct)
           - On 200: parse JSON if Content-Type indicates JSON, else return null

     Endpoint shapes (from source-doc §19 H):
       GET {EX_API}/orders/download
         ← query: from (yyyy-mm-dd), to (yyyy-mm-dd), filters[order_id] (comma-separated IDs), per_page (number)
         → 200: text/csv blob (saved by FE as "selected-orders.csv")

       POST {ORDERS_REST}/orders-by-client-id/{clientOrderID}/documents
         ← multipart/form-data fields: file (binary), document_type ("proof_of_delivery"), admin_override ("true")
         → 200: empty body (FE checks response.ok only) — but our tool returns whatever upstream sends (typically `{ document_id }` per Quiqup's documented Orders Core contract); future tightening welcome. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Ex-core client + download_orders_export (ORDL-07, CSV base64)</name>
  <files>lib/clients/ex-core.ts, lib/tools/download-orders-export.ts, tests/clients/ex-core.test.ts</files>
  <read_first>
    - lib/clients/quiqup-lastmile.ts (canonical binary handling — see the base64 envelope branch at the bottom of `request()`; mirror this verbatim)
    - lib/clients/quiqup-env.ts (env-var override convention)
    - lib/quiqup.ts (getQuiqupReadyJwt — Ex-core uses the same Quiqup gateway, same Bearer)
    - lib/tools/register.ts (ToolSpec)
    - docs/quiqup-api-full-frontend-extract.md lines 14-46 (service host map showing Ex-core uses VITE_EX_API_BASE_URL and is a Quiqup-gateway service — Bearer applies) + lines 4719-4721 (orders/download CSV shape)
    - tests/tools/google-places.test.ts (MSW-with-non-JSON-response pattern — relevant for CSV body assertion)
  </read_first>
  <action>
    Create `lib/clients/ex-core.ts`. Header comment must explain:
      - This is the Ex-core (legacy Ruby `ex-core`) client. Source-doc §1 line 18 names it as a distinct host with env-var family `VITE_EX_API_BASE_URL` on the FE side; we mirror that with `EX_API_BASE_URL` / `EX_API_STAGING_BASE_URL` on the server.
      - Auth: uses the SAME Clerk → Quiqup Bearer-JWT bridge as the Last-Mile / Platform / Quiqup REST clients — Ex-core sits behind the Quiqup gateway, NOT an external service. This is NOT an auth-exception client (Audit + Google Places are the two exceptions).
      - Binary handling: this host returns CSV bodies (and may return PDF / other binary in future). The client inspects Content-Type and returns the base64 envelope shape `{ contentType, base64 }` for non-JSON responses. Tools layer the `filenameHint` on top.

    Exports:
      - `EX_CORE_BASE_URLS = { production: "https://ex-api.quiqup.com", staging: "https://ex-api.staging.quiqup.com" } as const`.
      - `getExCoreBaseUrl(env: QuiqupEnvironment = "production"): string` — overrides via `EX_API_BASE_URL` / `EX_API_STAGING_BASE_URL`.
      - `ExCoreError` class — `{ status, body, name: "ExCoreError" }` shape mirroring `QuiqupHttpError`. Separate error class is the simpler call (Ex-core is a distinct service host with a separate operational backstop on the Quiqup side, even if the auth bridge is shared).
      - `ExCoreClient` class. Constructor opts `{ jwt, baseUrl?, environment? }`. `async request(method: HttpMethod, path: string, init?: { query? }): Promise<unknown>`:
        * Build URL via `new URL` + URLSearchParams (DO NOT use string concatenation on the query — keys like `filters[order_id]` need bracket-safe encoding, exactly the case `quiqupLastmileGet` handles).
        * Headers: `Authorization: Bearer ${jwt}`, `Accept: */*` (NOT `application/json` — Ex-core returns text/csv on the download endpoint).
        * On non-2xx → `throw new ExCoreError(res.status, await res.text())`.
        * On 200: check `res.headers.get("content-type") ?? ""`. If it includes `application/json` → return `res.json()`. Otherwise → read arraybuffer + return `{ contentType, base64: Buffer.from(buf).toString("base64") }` (mirror the QuiqupLastmileClient.request binary branch verbatim).

    Create `lib/tools/download-orders-export.ts`. `name: "download_orders_export"`. ORDL-07 → `GET /orders/download` on Ex-core. Input schema:
      ```
      z.object({
        from: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).describe("Inclusive start date in yyyy-mm-dd format (UTC). The upstream uses yyyy-mm-dd NOT full ISO-8601 — per source-doc §19 H line 4720."),
        to: z.string().regex(/^\d{4}-\d{2}-\d{2}$/).describe("Inclusive end date in yyyy-mm-dd format (UTC). Match the format of `from`."),
        order_ids: z.array(z.union([z.number().int().positive(), z.string().min(1)])).max(500).optional().describe("Optional filter — restrict the export to these clientOrderIDs. Wire-format is comma-separated via the `filters[order_id]` query key. Capped at 500 to bound the upstream cost."),
        per_page: z.number().int().min(1).max(5000).default(1000).describe("Rows per page (1-5000). Default 1000. CSV streams all rows in a single response — pagination here is the upstream's batch-size hint, NOT cursor pagination."),
        environment: environmentField,
      })
      ```
    Output schema: `z.object({ contentType: z.string(), base64: z.string(), filenameHint: z.string() }).passthrough()`. Handler:
      1. Reject when `!auth.userId`.
      2. Mint JWT.
      3. Instantiate `new ExCoreClient({ jwt, environment: args.environment })`.
      4. Build query — `from`, `to`, `per_page: String(args.per_page)` ALWAYS; `filters[order_id]: args.order_ids.join(",")` ONLY when order_ids is non-empty (the BE may treat empty string as "filter by nothing matches" — safer to omit).
      5. Call `client.request("GET", "/orders/download", { query })`.
      6. The result is either the base64 envelope (happy path) OR a JSON error object (5xx body shape). If `result.base64` is present, layer on `filenameHint: \`orders-export-\${args.from}-to-\${args.to}.csv\`` and return. If not, return the JSON as a text content block.

    Description (eval-driven-description bar):
      - Endpoint statement: "GET /orders/download (Ex-core host — ex-api.quiqup.com)."
      - Output contract: "Returns the canonical binary envelope `{ contentType: 'text/csv', base64: <body bytes base64-encoded>, filenameHint: 'orders-export-<from>-to-<to>.csv' }`. Decode the `base64` field client-side to get the CSV bytes; do NOT attempt to parse or summarise the CSV inside the agent — hand the file to the user verbatim, the same way `get_lastmile_order_label` returns a clickable URL."
      - Input semantics: name every field (`from`, `to`, `order_ids` optional, `per_page` defaulting to 1000), with the source-doc-locked yyyy-mm-dd date format.
      - When-to-use: "Use to export a CSV of orders matching a date range (and optionally specific IDs). For per-order detail in JSON, use `lookup_orders_ids` + `bulk_orders_lookup` instead."
      - Error modes: 401/403 → run `whoami_platform`; 422 → likely a bad date format or invalid order_ids list; 5xx → upstream retry.
      - Example: `{ "from": "2026-05-01", "to": "2026-05-19", "order_ids": [12345, 12346], "per_page": 1000, "environment": "production" }`.

    No `guardrails` block — read-only (it's an export, not a mutation).

    Create `tests/clients/ex-core.test.ts`. WR-05 cleanup: `EX_API_BASE_URL` AND `EX_API_STAGING_BASE_URL` in beforeEach/afterEach. Tests:
      - `it("sends Authorization: Bearer header")` — MSW captures request; assert header starts with "Bearer ".
      - `it("returns base64 envelope for text/csv responses")` — MSW returns Content-Type text/csv + body bytes; assert result.contentType === "text/csv" AND result.base64 round-trips back to the input bytes via Buffer.from(...).toString("base64").
      - `it("parses JSON when Content-Type is application/json")` — MSW returns `{ ok: true }` with application/json; assert result deep-equals.
      - `it("throws ExCoreError on non-2xx")` — MSW returns 502; assert `rejects.toBeInstanceOf(ExCoreError)`.
      - `it("honours EX_API_BASE_URL env override")` — set override, instantiate without baseUrl, MSW handler matches.
      - `it("encodes bracket-style filter query keys")` — query `{ "filters[order_id]": "1,2,3" }`; assert the captured request URL has the percent-encoded key (`filters%5Border_id%5D`) AND `decodeURIComponent` round-trips to the literal `filters[order_id]`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run tests/clients/ex-core.test.ts && grep -c "EX_API_BASE_URL\|EX_API_STAGING_BASE_URL" lib/clients/ex-core.ts && grep -c "Bearer" lib/clients/ex-core.ts && grep -c "base64" lib/clients/ex-core.ts lib/tools/download-orders-export.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/clients/ex-core.ts` exists; exports `ExCoreClient`, `ExCoreError`, `getExCoreBaseUrl`, `EX_CORE_BASE_URLS`.
    - `grep -c "EX_API_BASE_URL\|EX_API_STAGING_BASE_URL" lib/clients/ex-core.ts` >= 2.
    - `grep -c "Bearer" lib/clients/ex-core.ts` >= 1.
    - `grep -c "base64" lib/clients/ex-core.ts` >= 1 AND `grep -c "base64" lib/tools/download-orders-export.ts` >= 1 (binary contract present in both client and tool).
    - `lib/tools/download-orders-export.ts` exists; exports `spec: ToolSpec<...>`.
    - `grep -c "getQuiqupReadyJwt(auth.userId)" lib/tools/download-orders-export.ts` >= 1.
    - `grep -c "filenameHint" lib/tools/download-orders-export.ts` >= 1.
    - `grep -c "guardrails:" lib/tools/download-orders-export.ts` equals 0 (read-only — export, not mutation).
    - `pnpm vitest run tests/clients/ex-core.test.ts` exits 0.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Ex-core client lands with explicit env wiring + Bearer-JWT + binary base64 envelope. download_orders_export reaches Ex-core, returns the canonical `{ contentType, base64, filenameHint }` shape, and enforces yyyy-mm-dd date format on `from` / `to` per the upstream contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the Orders Core REST client + upload_order_document (ORDS-08, multipart write)</name>
  <files>lib/clients/orders-core-rest.ts, lib/tools/upload-order-document.ts, tests/clients/orders-core-rest.test.ts</files>
  <read_first>
    - lib/clients/quiqup-lastmile.ts (structural template — Bearer JWT, env-keyed baseUrl, QuiqupHttpError reuse)
    - lib/clients/orders-core-graphql.ts (sibling client created in 03-01 — env var name pattern; also note the fallback-chain rationale we extend here)
    - lib/tools/bulk-validate-products.ts (canonical base64-input pattern + JSON body — closest existing analog though Orders REST uses multipart not JSON)
    - lib/tools/update-account.ts (canonical write-tool pattern AFTER BL-01 fix — guardrails block shape, idempotency_key field, audit:true)
    - lib/tools/repair-integration-orders.ts (write tool with audit:true + idempotency_key + rate-limit — Phase 2 guardrails reference)
    - lib/quiqup.ts (getQuiqupReadyJwt)
    - docs/quiqup-api-full-frontend-extract.md §1 line 21 (fallback chain documentation) + §5 line 355 + §19 H lines 4671-4675 (multipart form fields)
  </read_first>
  <action>
    Create `lib/clients/orders-core-rest.ts`. Header comment must explain:
      - This is the Orders Core REST client, a sibling of the Orders Core GraphQL client created in 03-01. Same upstream BE service, different protocol/route.
      - Auth: SAME Clerk → Quiqup Bearer-JWT bridge — not an auth exception.
      - Error model: reuses `QuiqupHttpError` from `./quiqup-lastmile` (Orders Core IS a Quiqup-prefixed service).
      - Fallback chain (CRITICAL): the FE derives this host from `VITE_ORDERS_API_BASE_URL` with fallback to `ORDERS_API_GRAPH_URL` minus `/graph` (source-doc §1 line 21). We preserve this: the resolver checks ORDERS_API_BASE_URL first, then falls back to QUIQUP_ORDERS_GRAPH_URL with `/graph` stripped, then the canonical URL. Rationale: in dev, setting a single env var (`QUIQUP_ORDERS_GRAPH_URL=https://localhost.test/graph`) redirects BOTH the GraphQL and REST surfaces to the same dev host — matches FE ergonomics.
      - Multipart: `requestMultipart` builds a `FormData` body. Critically: DO NOT set the `Content-Type` header manually — fetch() will set `multipart/form-data; boundary=...` automatically when given a FormData body. Setting it manually clobbers the boundary and the upstream rejects the body.

    Exports:
      - `ORDERS_REST_BASE_URLS = { production: "https://orders-api.quiqup.com", staging: "https://orders-api.staging.quiqup.com" } as const`.
      - `getOrdersRestBaseUrl(env: QuiqupEnvironment = "production"): string` — implements the fallback chain documented above. For staging: check `process.env.ORDERS_API_STAGING_BASE_URL`, fallback to `process.env.QUIQUP_ORDERS_GRAPH_STAGING_URL` minus `/graph` (i.e. `.replace(/\/graph$/, "")` to be exact), fallback to canonical staging URL. Same shape for production.
      - `OrdersCoreRestClient` class. Constructor `{ jwt, baseUrl?, environment? }`. Methods:
        * `async request(method: HttpMethod, path: string, init?: { body?, query? }): Promise<unknown>` — generic JSON request with Bearer header, throws QuiqupHttpError on non-2xx, parses JSON on 200.
        * `async requestMultipart(method: HttpMethod, path: string, formData: FormData): Promise<unknown>` — multipart POST. Headers: `Authorization: Bearer ${jwt}`, `Accept: application/json`. NO `Content-Type` header (per the comment above). Throws QuiqupHttpError on non-2xx. On 200, parses JSON if content-type indicates JSON; else returns `null` (matches the FE's "checks response.ok only" expectation per source-doc line 4674).

    Create `lib/tools/upload-order-document.ts`. `name: "upload_order_document"`. ORDS-08 → `POST /orders-by-client-id/{clientOrderID}/documents`. Input schema:
      ```
      z.object({
        client_order_id: z.union([z.number().int().positive(), z.string().min(1)]).describe("Quiqup clientOrderID — accepts either the integer ID or its string form. encodeURIComponent is applied at the URL boundary so caller-supplied IDs cannot inject path components."),
        file_base64: z.string().min(1).describe("Base64-encoded file bytes. PDF / JPEG / PNG are typical proof-of-delivery payloads. Cap at ~10MB (~13.5M base64 chars) — agents trying to upload very large blobs should split first."),
        filename: z.string().min(1).max(255).describe("Original filename (informational; the upstream stores it on the document record). Strip path separators server-side."),
        content_type: z.string().min(1).describe("MIME type of the file (e.g. 'image/jpeg', 'image/png', 'application/pdf'). Used to populate the multipart part's Content-Type."),
        document_type: z.string().min(1).default("proof_of_delivery").describe("Document type tag. Source-doc §19 H line 4673 shows 'proof_of_delivery' as the observed FE value. Defaults to that; pass another string only if you know the upstream supports it."),
        admin_override: z.boolean().optional().default(true).describe("Boolean flag that the FE always sends as 'true' (source-doc §19 H line 4673). Defaults to true — pass false to skip the admin-override path if upstream policy permits it."),
        idempotency_key: z.string().optional().describe("Optional caller-supplied key to dedupe retries within a 15-minute window. Wire-shape: this field is NOT included in the multipart envelope — it's consumed by the registerTool wrapper for the idempotency cache key."),
        environment: environmentField,
      })
      ```

      IMPORTANT — server-side identity binding (BL-04 lesson):
      The input schema has NO `user_id`, `actor_id`, `actor_email`, or `partner_id` field. The identity binding is derived exclusively from `auth.userId` at the handler level. If the upstream Orders Core requires an actor identifier on the form (verify against the actual upstream contract during execution), the handler MUST mint it from `auth.userId` (e.g. via the Clerk session lookup), NOT accept a caller-supplied value. Caller-controlled identity is a cross-tenant smuggling vector.

      Cap on file size: enforce `args.file_base64.length <= 13_500_000` (≈10MB after base64 expansion). Throw `Error("file_base64 exceeds 10MB cap")` if exceeded — BEFORE constructing the FormData, BEFORE minting the JWT, so abusive callers cost nothing upstream.

    Output schema: `z.object({}).passthrough()`. Handler:
      1. Reject when `!auth.userId`.
      2. Enforce the 10MB cap (above).
      3. Filename hygiene: strip path separators — `const safeFilename = args.filename.replace(/[\\/]/g, "_")`.
      4. Mint JWT.
      5. Build FormData: `const fd = new FormData(); fd.append("file", new Blob([Buffer.from(args.file_base64, "base64")], { type: args.content_type }), safeFilename); fd.append("document_type", args.document_type); fd.append("admin_override", String(args.admin_override));`.
      6. `const client = new OrdersCoreRestClient({ jwt, environment: args.environment });`.
      7. `const data = await client.requestMultipart("POST", \`/orders-by-client-id/\${encodeURIComponent(String(args.client_order_id))}/documents\`, fd);`.
      8. Return `{ content: [{ type: "text" as const, text: JSON.stringify(data, null, 2) }] }`.

    BL-01 guardrails block (CANONICAL — copy from update-account.ts / repair-integration-orders.ts):
      ```
      guardrails: {
        rateLimit: { capacity: 10, refillPerSec: 10 / 60 },   // 10/min — uploads should be rare; runaway rate suggests misuse
        idempotency: { keyArg: "idempotency_key", ttlMs: 15 * 60 * 1000 },
        audit: true,
      }
      ```

    Description (eval-driven-description bar):
      - Endpoint statement: "POST /orders-by-client-id/{clientOrderID}/documents (Orders Core REST host — orders-api.quiqup.com; multipart/form-data)."
      - Input contract: name every field with semantics; explicitly note the file_base64 + content_type + filename pattern (canonical MCP file-upload triple).
      - Identity binding note: "This tool does NOT accept caller-supplied user/actor fields. The uploader identity is bound server-side to the authenticated user. Do not pass `user_id`, `actor_id`, or `actor_email` — they will be ignored at best and rejected at worst."
      - Output contract: "Returns the Orders Core document reference (typical shape `{ document_id, document_type, created_at, url }` — verify against the actual upstream response). The FE-side hook checks only `response.ok` (source-doc line 4674) so undocumented fields may appear."
      - Idempotency note: explain that idempotency_key lets the agent retry safely within 15 minutes.
      - Error modes: 401/403 → run `whoami_platform`; 413 → file too large (likely > upstream's accepted cap, even if under our 10MB client cap); 422 → bad client_order_id or unsupported document_type; 5xx → upstream retry.
      - Example: `{ "client_order_id": 12345, "file_base64": "iVBORw0KGgo...", "filename": "pod-12345.jpg", "content_type": "image/jpeg", "document_type": "proof_of_delivery", "admin_override": true, "environment": "production" }`.

    Create `tests/clients/orders-core-rest.test.ts`. WR-05 cleanup: capture+restore `ORDERS_API_BASE_URL`, `ORDERS_API_STAGING_BASE_URL`, `QUIQUP_ORDERS_GRAPH_URL`, `QUIQUP_ORDERS_GRAPH_STAGING_URL` (all FOUR env-var keys, because the fallback chain consults two of them — WR-05 cleanup must cover the whole chain). Tests:
      - `it("requestMultipart sends Bearer header WITHOUT a manual Content-Type")` — MSW captures the outbound request; assert Authorization starts with "Bearer "; assert `request.headers.get("Content-Type")?.startsWith("multipart/form-data") === true` (fetch sets this) AND verify the form-data boundary is present.
      - `it("requestMultipart forwards form fields")` — MSW captures the body (use `request.formData()`); assert fields `file`, `document_type`, `admin_override` are present.
      - `it("throws QuiqupHttpError on non-2xx")` — MSW returns 422; assert the thrown error is `QuiqupHttpError`.
      - `it("falls back to ORDERS_API_GRAPH_URL minus /graph when ORDERS_API_BASE_URL is unset")` — clear all env vars, set `process.env.QUIQUP_ORDERS_GRAPH_URL = "https://localhost.test/graph"`, instantiate the client without baseUrl, assert outbound MSW handler matches `https://localhost.test` (the trailing `/graph` is stripped).
      - `it("honours ORDERS_API_BASE_URL when set")` — set `ORDERS_API_BASE_URL = "https://direct-rest.test"`, assert MSW handler matches.
      - `it("environment: staging routes to staging cluster")` — instantiate with `environment: "staging"`, MSW handler matches `https://orders-api.staging.quiqup.com`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run tests/clients/orders-core-rest.test.ts && grep -c "guardrails:" lib/tools/upload-order-document.ts && grep -c "audit: true" lib/tools/upload-order-document.ts && grep -c "FormData" lib/tools/upload-order-document.ts && grep -c "user_id\|actor_id\|actor_email" lib/tools/upload-order-document.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/clients/orders-core-rest.ts` exists; exports `OrdersCoreRestClient`, `getOrdersRestBaseUrl`, `ORDERS_REST_BASE_URLS`.
    - `grep -c "ORDERS_API_BASE_URL\|ORDERS_API_STAGING_BASE_URL" lib/clients/orders-core-rest.ts` >= 2.
    - `grep -c "QUIQUP_ORDERS_GRAPH_URL\|QUIQUP_ORDERS_GRAPH_STAGING_URL" lib/clients/orders-core-rest.ts` >= 2 (fallback chain to the 03-01 client's env var is wired).
    - `lib/clients/orders-core-rest.ts` MUST NOT set a manual Content-Type on the multipart path — verify by `grep -c '"Content-Type":' lib/clients/orders-core-rest.ts` equals 0 inside requestMultipart (we test this at runtime too).
    - `lib/tools/upload-order-document.ts` exists; exports `spec: ToolSpec<...>`.
    - `grep -c "getQuiqupReadyJwt(auth.userId)" lib/tools/upload-order-document.ts` >= 1.
    - `grep -c "guardrails:" lib/tools/upload-order-document.ts` >= 1 (BL-01 canonical).
    - `grep -c "audit: true" lib/tools/upload-order-document.ts` >= 1.
    - `grep -c "idempotency_key" lib/tools/upload-order-document.ts` >= 2 (input field + guardrails keyArg).
    - `grep -c "FormData" lib/tools/upload-order-document.ts` >= 1.
    - `grep -c "encodeURIComponent" lib/tools/upload-order-document.ts` >= 1 (path-param hygiene on client_order_id).
    - `grep -v '^//' lib/tools/upload-order-document.ts | grep -v '^ \*' | grep -c "user_id\|actor_id\|actor_email"` equals 0 (BL-04 server-binding — schema MUST NOT accept caller-supplied identity).
    - `grep -c "13_500_000\|13500000" lib/tools/upload-order-document.ts` >= 1 (10MB cap enforced).
    - `tests/clients/orders-core-rest.test.ts` exists with >= 6 `it()` cases.
    - `pnpm vitest run tests/clients/orders-core-rest.test.ts` exits 0.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Orders Core REST client lands with the FE-aligned fallback chain (ORDERS_API_BASE_URL → QUIQUP_ORDERS_GRAPH_URL minus /graph → canonical) AND proper multipart-without-manual-Content-Type semantics. upload_order_document is a BL-01-canonical write tool (guardrails, audit:true, idempotency_key), BL-04-compliant (no caller-supplied identity), with a 10MB pre-flight cap and path/filename hygiene.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tool tests + route registration + snapshot bump for both tools</name>
  <files>tests/tools/orders-export-and-upload.test.ts, app/[transport]/route.ts, evals/snapshots/tool-surface.json</files>
  <read_first>
    - tests/tools/auth-account-reads.test.ts (WR-05 env-cleanup pattern; 3-assertion read template)
    - tests/tools/integrations-shared.test.ts (write-tool body inspection + schema-parse tests + URLSearchParams URL inspection)
    - tests/tools/bulk-validate-products.test.ts (if it exists — confirm via `ls tests/tools/`) — pattern for base64-input tests
    - app/[transport]/route.ts (find Phase 3 Wave-3 block from 03-03; new block goes after)
    - evals/snapshots/tool-surface.json
  </read_first>
  <action>
    Step A — Create `tests/tools/orders-export-and-upload.test.ts`. WR-05 cleanup for ALL relevant env vars in beforeEach/afterEach: `EX_API_BASE_URL`, `EX_API_STAGING_BASE_URL`, `ORDERS_API_BASE_URL`, `ORDERS_API_STAGING_BASE_URL`, `QUIQUP_ORDERS_GRAPH_URL`, `QUIQUP_ORDERS_GRAPH_STAGING_URL`. Two describe blocks:

      - describe("download_orders_export"):
        * `it("happy path returns the base64 envelope")` — MSW returns text/csv body; assert `result.content[0].text` parses to JSON containing `contentType: "text/csv"`, a non-empty `base64` field, and a `filenameHint` matching `/orders-export-2026-05-01-to-2026-05-19\.csv$/`.
        * `it("forwards from/to/per_page query params")` — assert MSW saw `searchParams.get("from") === "2026-05-01"`, etc.
        * `it("encodes filters[order_id] when order_ids is non-empty")` — pass order_ids `[1,2,3]`; assert MSW URL contains the percent-encoded `filters%5Border_id%5D=1%2C2%2C3` AND that `decodeURIComponent` recovers `filters[order_id]=1,2,3`.
        * `it("omits filters[order_id] when order_ids is undefined")` — MSW captures URL; assert no `filters` key in searchParams.
        * `it("rejects bad date format")` — `spec.inputSchema.safeParse({ from: "2026/05/01", to: "2026-05-19" }).success === false`.
        * `it("rejects unauthenticated callers")` — same pattern as Phase 2.

      - describe("upload_order_document"):
        * `it("happy path returns the document reference")` — MSW returns `{ document_id: "doc-1" }`; assert result text contains `"document_id": "doc-1"`.
        * `it("sends multipart body with file + document_type + admin_override fields")` — MSW captures the request; use `request.formData()` to assert the three fields are present AND `request.headers.get("Content-Type")?.startsWith("multipart/form-data") === true`.
        * `it("encodes client_order_id in the path")` — pass `client_order_id: "12/345"`; assert MSW URL path is `/orders-by-client-id/12%2F345/documents`.
        * `it("strips path separators from filename")` — pass `filename: "../../etc/passwd"`; assert MSW form-data `file` field's filename is `".._.._etc_passwd"` (every `/` and `\` replaced with `_`).
        * `it("rejects file_base64 > 10MB cap")` — pass a base64 string of length 13_500_001; expect handler rejection BEFORE any MSW interception. (Hint: register MSW with an unreachable handler then assert MSW received zero requests via `server.events.on("request:start", ...)` or assert via spy on fetch.)
        * `it("does NOT accept caller-supplied user_id")` — `spec.inputSchema.safeParse({ ..., user_id: "evil-user" }).success === true` BUT after parse, `parsed.user_id === undefined` (Zod strips unknown keys by default on z.object). Even better: assert the spec input keys list does NOT contain "user_id" / "actor_id" / "actor_email" via `Object.keys(spec.inputSchema.shape)`.
        * `it("guardrails block sets audit:true + rate-limit + idempotency")` — read `spec.guardrails`; assert `audit === true`, `idempotency.keyArg === "idempotency_key"`, `rateLimit.capacity === 10`.
        * `it("rejects unauthenticated callers")` — same pattern.

    Step B — Edit `app/[transport]/route.ts`. Add a new comment block `// -- Phase 3: Orders read path — Ex-core CSV export + Orders Core REST multipart (ORDL-07/ORDS-08) --` placed after the Phase-3 Wave-3 block from 03-03. Imports + registers:
      ```
      import { spec as downloadOrdersExportSpec } from "@/lib/tools/download-orders-export";
      import { spec as uploadOrderDocumentSpec } from "@/lib/tools/upload-order-document";
      // ...
      registerTool(server, downloadOrdersExportSpec);
      registerTool(server, uploadOrderDocumentSpec);
      ```

    Step C — Update `evals/snapshots/tool-surface.json`. Add 2 entries under `tools` keyed by tool name with value `"enabled"`. Re-sort alphabetically. Smoke-test via `EVAL_GATE=1 bun run eval:tool-surface`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run tests/tools/orders-export-and-upload.test.ts tests/clients/ex-core.test.ts tests/clients/orders-core-rest.test.ts && EVAL_GATE=1 bun run eval:tool-surface && jq -r '.tools | keys[]' evals/snapshots/tool-surface.json | grep -c "^download_orders_export$\|^upload_order_document$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  describe(" tests/tools/orders-export-and-upload.test.ts` equals 2.
    - `grep -c "delete process.env.EX_API_BASE_URL\|delete process.env.ORDERS_API_BASE_URL" tests/tools/orders-export-and-upload.test.ts` >= 2 (WR-05 cleanup applied to BOTH new env-var families).
    - `grep -c "registerTool(server," app/[transport]/route.ts` increased by exactly 2 vs git HEAD.
    - `grep -c "downloadOrdersExportSpec\|uploadOrderDocumentSpec" app/[transport]/route.ts` >= 4.
    - `jq '.tools | keys | length' evals/snapshots/tool-surface.json` equals previous_count + 2.
    - `jq -r '.tools | keys[]' evals/snapshots/tool-surface.json | grep -c "^download_orders_export$\|^upload_order_document$"` equals 2.
    - `EVAL_GATE=1 bun run eval:tool-surface` exits 0.
    - `pnpm test` exits 0 (full suite — must not regress).
  </acceptance_criteria>
  <done>
    Both new tools are registered, snapshot-recorded, and covered by a 2-describe Vitest suite that validates the binary-envelope contract, the multipart-without-manual-Content-Type semantics, the 10MB cap, the path/filename hygiene, the no-caller-supplied-identity invariant, and the BL-01 guardrails block. Wave 4 of Phase 3 lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → Next.js MCP route | Untrusted inbound. Clerk gate is the only credential check. |
| MCP handler → ex-api.quiqup.com (Ex-core) | Egress over TLS with Bearer JWT via the Clerk → Quiqup bridge. |
| MCP handler → orders-api.quiqup.com (Orders Core REST) | Egress over TLS with Bearer JWT via the same bridge. Multipart body built server-side from caller-supplied file_base64. |
| Tool args (LLM-supplied) → upstream path/query/body | LLM-controlled values reaching upstream: dates (regex-validated), order_ids (capped), file_base64 (size-capped), client_order_id (path-encoded), filename (path-separator-stripped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Spoofing | upload_order_document caller-identity smuggle | mitigate | Input schema has NO user_id / actor_id / actor_email field (acceptance criteria grep-locked). Identity bound server-side to auth.userId. BL-04 lesson generalized. |
| T-03-24 | Tampering | client_order_id path injection | mitigate | encodeURIComponent applied before URL interpolation. Test asserts a path-character-containing id round-trips encoded. |
| T-03-25 | Tampering | filename path traversal | mitigate | Server-side `replace(/[\\/]/g, "_")` strips both `/` and `\`. Test asserts `"../../etc/passwd"` → `".._.._etc_passwd"`. The upstream's at-rest filename handling is not relied upon. |
| T-03-26 | Tampering | filters[order_id] injection | mitigate | order_ids is `z.array(z.union([z.number().int().positive(), z.string().min(1)])).max(500)` — caps the join() at 500 entries. URLSearchParams handles encoding deterministically. |
| T-03-27 | Denial of Service | Oversized file_base64 upload | mitigate | Handler enforces `file_base64.length <= 13_500_000` (~10MB after base64 expansion) BEFORE JWT mint and FormData construction. Combined with the upload guardrail rateLimit (10/min), a runaway agent costs the MCP at most ~100MB/minute in pre-flight checks (well below abuse threshold). |
| T-03-28 | Denial of Service | Repeated CSV exports | accept | download_orders_export is read-only; no rate-limit set (matches the read convention). The per_page cap (5000) and order_ids cap (500) bound per-call cost. |
| T-03-29 | Repudiation | Upload-tool audit trail | mitigate | upload_order_document carries `guardrails.audit: true` per the BL-01 canonical. registerTool wrapper emits a structured audit record on every call. PII-redact strips file_base64 + filename via ALWAYS_REDACT_KEYS (file_base64 is in the list per Phase 2 BL-02 work; filename is treated under the WHITELIST default for write tools). |
| T-03-30 | Information Disclosure | upstream document URL echo-back | accept | The upstream typically returns the new document's URL/id. Surfacing it to the agent is the documented success path. The PII risk on the URL itself (e.g. signed s3 URL) is the upstream's contract; the MCP does not re-filter. |
| T-03-31 | Tampering | manual Content-Type override breaking multipart boundary | mitigate | OrdersCoreRestClient.requestMultipart deliberately DOES NOT set Content-Type — fetch() sets `multipart/form-data; boundary=...` correctly. Acceptance criteria grep-locks this (zero `"Content-Type":` references in requestMultipart). Manual-set would clobber the boundary and the upstream would reject. |
| T-03-32 | Information Disclosure | Auth-bearing fields leaking through output schema | mitigate | Output schemas on both tools are `z.object({}).passthrough()` but the upstream is the source of fields — none of the documented response shapes carry `token`/`bearer`/`jwt`/`code`. ALWAYS_REDACT_KEYS provides belt-and-braces at the audit layer. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. Uses existing zod + msw + stdlib `fetch` + `FormData` + `Blob` (both globals in Node 20+). RESEARCH.md skipped per phase init. |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0.
- `pnpm vitest run tests/clients/ex-core.test.ts tests/clients/orders-core-rest.test.ts tests/tools/orders-export-and-upload.test.ts` exits 0.
- `EVAL_GATE=1 bun run eval:tool-surface` exits 0 (snapshot includes the 2 new tools).
- `pnpm test` exits 0 (full suite — must not regress).
- `grep -v '^//' lib/tools/upload-order-document.ts | grep -v '^ \*' | grep -c "user_id\|actor_id\|actor_email"` equals 0 (server-binding invariant).
</verification>

<success_criteria>
1. `lib/clients/ex-core.ts` exists with explicit `EX_API_BASE_URL`/`EX_API_STAGING_BASE_URL` env wiring, Bearer-JWT, and base64 envelope for non-JSON responses.
2. `lib/clients/orders-core-rest.ts` exists with the documented fallback chain (ORDERS_API_BASE_URL → QUIQUP_ORDERS_GRAPH_URL minus /graph → canonical), Bearer-JWT, AND a `requestMultipart` method that does NOT set Content-Type manually.
3. `download_orders_export` (ORDL-07) returns `{ contentType, base64, filenameHint }` matching the binary-response contract.
4. `upload_order_document` (ORDS-08) carries the BL-01 canonical guardrails block, enforces a 10MB file cap, strips filename path separators, and has NO caller-supplied identity field (BL-04 server-binding).
5. All test files use the WR-05 `delete process.env.*` cleanup for the four new env-var families (EX_API_*, ORDERS_API_*, and the two QUIQUP_ORDERS_GRAPH_* fallback keys).
6. `app/[transport]/route.ts` registers both new specs under a Phase-3 Wave-4 comment block.
7. `evals/snapshots/tool-surface.json` includes both tools as `enabled`.
</success_criteria>

<output>
Create `.planning/phases/03-orders-read-path/03-04-SUMMARY.md` when done.
</output>
