---
phase: 04-orders-write-path-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - lib/tools/export-order.ts
  - lib/tools/update-fulfilment-order-status.ts
  - lib/tools/create-order-charge.ts
  - lib/tools/update-order-weight.ts
  - tests/tools/single-order-mutations.test.ts
  - app/[transport]/route.ts
  - evals/snapshots/tool-surface.json
  - .planning/phases/04-orders-write-path-lifecycle/CALL-LOG-wave-03.md
autonomous: true
requirements:
  - ORDS-03
  - ORDS-04
  - ORDS-06
  - ORDS-07

must_haves:
  truths:
    - "Agent can `export_order` (ORDS-03) — PUTs to Quiqup REST `/orders/export/{id}` and gets the upstream response."
    - "Agent can `update_fulfilment_order_status` (ORDS-04) — PATCHes Platform `/api/fulfilment/orders/{id}` ONLY with `confirm: true` (DESTRUCTIVE-gated per D-06); response contains the updated status."
    - "Agent can `create_order_charge` (ORDS-06) — POSTs Platform `/quiqdash/order-charge` with amount + order id."
    - "Agent can `update_order_weight` (ORDS-07) — PATCHes Platform `/quiqdash/orders/{orderId}/weight` with weight in a sane numeric range."
    - "Both `update_fulfilment_order_status` and the three non-gated single-order tools carry per-order scope assertion before the mutation."
  artifacts:
    - path: "lib/tools/export-order.ts"
      provides: "ORDS-03 — PUT /orders/export/{id} (Quiqup REST)"
      contains: "QuiqupLastmileClient"
    - path: "lib/tools/update-fulfilment-order-status.ts"
      provides: "ORDS-04 — PATCH /api/fulfilment/orders/{id} (Platform, DESTRUCTIVE-gated)"
      contains: "destructiveConfirmField"
    - path: "lib/tools/create-order-charge.ts"
      provides: "ORDS-06 — POST /quiqdash/order-charge (Platform)"
      contains: "PlatformApiClient"
    - path: "lib/tools/update-order-weight.ts"
      provides: "ORDS-07 — PATCH /quiqdash/orders/{orderId}/weight (Platform)"
      contains: "PlatformApiClient"
    - path: ".planning/phases/04-orders-write-path-lifecycle/CALL-LOG-wave-03.md"
      provides: "Wave 3 live-staging CALL-LOG — one section per tool"
      contains: "export_order"
  key_links:
    - from: "lib/tools/update-fulfilment-order-status.ts"
      to: "lib/middleware/destructive.ts"
      via: "import of requireConfirm + destructive fields (D-06: this single tool is DESTRUCTIVE-gated)"
      pattern: "from \"@/lib/middleware/destructive\""
    - from: "lib/tools/export-order.ts AND create-order-charge.ts AND update-order-weight.ts"
      to: "lib/middleware/scope.ts"
      via: "assertOrderBelongsToUser before mutation"
      pattern: "assertOrderBelongsToUser"
---

<objective>
Ship the 4 single-order mutation tools. These are direct handler implementations (NOT factory-driven) because each has a distinct shape, but they share scope-assertion and (for ORDS-04 only) the canonical destructive gate.

Purpose: Cover the order-mutation surface beyond batch transitions. The four tools span three upstream surfaces (Quiqup REST PUT, Platform PATCH/POST, Platform PATCH-with-numeric-bound).

Output: 4 per-tool files + a single combined test file + route wiring + snapshot bump + Wave-3 CALL-LOG.md.
</objective>

<execution_context>
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md
@AGENTS.md
@lib/middleware/destructive.ts
@lib/middleware/scope.ts
@lib/clients/platform-api.ts
@lib/clients/quiqup-rest.ts
@lib/tools/cancel-lastmile-orders-batch.ts
</context>

<interfaces>
From `lib/clients/platform-api.ts`:
- `class PlatformApiClient` with `request(method, path, opts)` — used by ORDS-04 (PATCH), ORDS-06 (POST), ORDS-07 (PATCH).

From `lib/clients/quiqup-rest.ts`:
- Quiqup REST client (api-ae.quiqup.com surface) — used by ORDS-03 export_order PUT. Read this file to confirm exact class/method name. If it does not exist as `quiqup-rest.ts` (Phase 3 added it for `get_order_history`), check `lib/clients/quiqup-lastmile.ts` — they may share a base.

From `lib/middleware/destructive.ts`:
- All 6 canonical exports — ORDS-04 imports them directly per D-06.

From `lib/middleware/scope.ts`:
- `assertOrderBelongsToUser(orderId, userId)` — every single-order tool calls this before the mutation.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship `export_order` (ORDS-03) + `update_fulfilment_order_status` (ORDS-04)</name>
  <files>lib/tools/export-order.ts, lib/tools/update-fulfilment-order-status.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 86-87 (ORDS-03, ORDS-04 specs)
    - lib/clients/quiqup-rest.ts (Quiqup REST client — confirm export class/method)
    - lib/clients/platform-api.ts (PlatformApiClient.request signature for PATCH)
    - lib/middleware/destructive.ts (canonical helpers for ORDS-04 gate)
    - lib/middleware/scope.ts (assertOrderBelongsToUser for both tools)
    - lib/tools/delete-salla-connection.ts (canonical single-resource destructive analog for ORDS-04 shape)
    - lib/tools/upload-order-document.ts (canonical Platform-PATCH write tool — guardrails block pattern)
    - .planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md decision 6 (update_fulfilment_order_status is DESTRUCTIVE-gated)
  </read_first>
  <behavior>
    - `export_order` Test 1: inputSchema contains `order_id` (string min 1), `idempotency_key` (optional), `environment`. NO confirm/dry_run (not destructive — export is read-shaped despite being a PUT per upstream's quirky API).
    - `export_order` Test 2: handler calls `assertOrderBelongsToUser` before the PUT; mocked denial returns scope-violation refusal.
    - `export_order` Test 3: happy path PUTs to `/orders/export/<encoded-id>` via the Quiqup REST client; returns upstream response.
    - `export_order` Test 4: order_id with `/` is URL-encoded in the outbound path.
    - `update_fulfilment_order_status` Test 5: inputSchema contains `order_id` (string), `status` (string min 1 — free-form per the precedent established in D-02 for state taxonomies; description names the relevant enumeration if any), `confirm` (destructiveConfirmField), `dry_run` (destructiveDryRunField), `idempotency_key`, `environment`.
    - `update_fulfilment_order_status` Test 6: `confirm` missing → ConfirmationRequiredError; ZERO PATCH.
    - `update_fulfilment_order_status` Test 7: `confirm: true` + `dry_run: true` → response contains `"dryRun": true` and `"orderId":` and the requested `status`; ZERO PATCH.
    - `update_fulfilment_order_status` Test 8: `confirm: true`, in-scope → PATCH to `/api/fulfilment/orders/<encoded-id>` with body `{ status: "<value>" }`.
    - `update_fulfilment_order_status` Test 9: out-of-scope → refusal; ZERO PATCH.
    - Both tools Test 10: `!auth.userId` throws before any work.
  </behavior>
  <action>
    Create `lib/tools/export-order.ts`:
    - inputSchema (z.object): `order_id: z.string().min(1)`, `idempotency_key: z.string().optional()`, `environment: environmentField`.
    - outputSchema: `z.object({}).passthrough()`.
    - description: "Trigger Quiqup's order-export side-effect for a single order. Despite being an HTTP PUT, this is a read-shaped operation (it requests a re-export of order data to integrations) — not destructive. Per-order scope-checked under the caller's session before firing."
    - guardrails: `{ rateLimit: { capacity: 10, refillPerSec: 10 / 60 }, idempotency: { keyArg: "idempotency_key", ttlMs: 15 * 60 * 1000 }, audit: true }` (NOT the tight 3/min — this is non-destructive).
    - handler: auth → assertOrderBelongsToUser → mint JWT → Quiqup REST client PUT `/orders/export/${encodeURIComponent(args.order_id)}` with empty body → return upstream payload.

    Create `lib/tools/update-fulfilment-order-status.ts`:
    - inputSchema (z.object): `order_id: z.string().min(1)`, `status: z.string().min(1).describe("Target fulfilment status. Free-form string accepted by the upstream — the BE owns the valid set. If a Phase-1 enumeration tool exists for fulfilment statuses, call it first; otherwise consult the BE schema or order-history audit events for observed values.")`, `confirm: destructiveConfirmField`, `dry_run: destructiveDryRunField`, `idempotency_key: z.string().optional()`, `environment: environmentField`.
    - outputSchema: `z.object({}).passthrough()`.
    - description: "DESTRUCTIVE state mutation: change a fulfilment order's status. Requires `confirm: true`. Use `dry_run: true` (paired with confirm) to preview. Has the same 'would-not-want-to-undo' property as batch transitions (D-06)."
    - guardrails: TIGHT 3/min canonical block (same as batch tools).
    - handler: auth → requireConfirm("update_fulfilment_order_status", args, `order ${args.order_id} → status "${args.status}"`) → assertOrderBelongsToUser → if isDryRun return synthesized `{ dryRun: true, orderId: args.order_id, simulated: { ok: true, order_id: args.order_id, status: args.status } }` → mint JWT → PlatformApiClient PATCH `/api/fulfilment/orders/${encodeURIComponent(args.order_id)}` with body `{ status: args.status }` → return upstream payload.

    Test file is created in Task 2 (combined for all 4 single-order tools).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls lib/tools/export-order.ts lib/tools/update-fulfilment-order-status.ts` returns both files
    - `grep -c 'destructiveConfirmField' lib/tools/export-order.ts == 0` (NOT destructive)
    - `grep -c 'destructiveConfirmField' lib/tools/update-fulfilment-order-status.ts == 1` (IS destructive per D-06)
    - `grep -c 'assertOrderBelongsToUser' lib/tools/export-order.ts == 1`
    - `grep -c 'assertOrderBelongsToUser' lib/tools/update-fulfilment-order-status.ts == 1`
    - `grep -c 'encodeURIComponent' lib/tools/export-order.ts >= 1`
    - `grep -c 'encodeURIComponent' lib/tools/update-fulfilment-order-status.ts >= 1`
    - `grep -c 'rateLimit: { capacity: 3, refillPerSec: 3 / 60 }' lib/tools/update-fulfilment-order-status.ts == 1`
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both tools compile; ORDS-03 is non-destructive with scope assertion; ORDS-04 is canonical destructive with scope assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship `create_order_charge` (ORDS-06) + `update_order_weight` (ORDS-07) + combined MSW tests + route wiring</name>
  <files>lib/tools/create-order-charge.ts, lib/tools/update-order-weight.ts, tests/tools/single-order-mutations.test.ts, app/[transport]/route.ts, evals/snapshots/tool-surface.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 89-90 (ORDS-06, ORDS-07 specs)
    - lib/tools/export-order.ts and lib/tools/update-fulfilment-order-status.ts (Task 1 output — same patterns)
    - lib/clients/platform-api.ts (POST + PATCH method signatures)
    - .planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md threat_model_guidance (T-04-create_order_charge amount upper bound; T-04-update_order_weight numeric range 0 < kg <= 1000)
  </read_first>
  <behavior>
    - `create_order_charge` Test 1: inputSchema contains `order_id: z.string().min(1)`, `amount: z.number().positive().max(<sane cap, e.g., 100_000>)`, `currency: z.string().min(3).max(8)` OR similar (read upstream contract from source-doc if unclear), `description: z.string().optional()`, `idempotency_key`, `environment`.
    - `create_order_charge` Test 2: assertOrderBelongsToUser called before POST.
    - `create_order_charge` Test 3: happy path POSTs to `/quiqdash/order-charge` with body containing order_id + amount + currency.
    - `create_order_charge` Test 4: `amount: -10` rejected at Zod parse.
    - `create_order_charge` Test 5: `amount: 1_000_000` (above cap) rejected at Zod parse — protects against runaway agent (per threat_model_guidance T-04-create_order_charge).
    - `update_order_weight` Test 6: inputSchema contains `order_id: z.string().min(1)`, `weight_kg: z.number().positive().max(1000)`, `idempotency_key`, `environment`. NO confirm (non-destructive, but rate-limited).
    - `update_order_weight` Test 7: `weight_kg: 0` rejected (must be positive).
    - `update_order_weight` Test 8: `weight_kg: 1001` rejected (above cap).
    - `update_order_weight` Test 9: `weight_kg: -5` rejected.
    - `update_order_weight` Test 10: happy path PATCHes to `/quiqdash/orders/<encoded-id>/weight` with body `{ weight_kg: <n> }` (or `{ weight: <n> }` — confirm exact wire-format field name from source-doc; if the upstream takes `weight`, translate inside handler).
    - `update_order_weight` Test 11: assertOrderBelongsToUser called before PATCH.
    - Route registration Test 12: All 4 single-order-mutation tools imported and registered in `app/[transport]/route.ts` under a Wave-3 block.
    - Snapshot Test 13: tool-surface.json contains all 4 new entries with `enabled: true`.
  </behavior>
  <action>
    Create `lib/tools/create-order-charge.ts`:
    - inputSchema: `order_id: z.string().min(1)`, `amount: z.number().positive().max(100_000).describe("Charge amount. Capped at 100,000 to prevent runaway-agent abuse (T-04 threat register). Currency interpretation determined by the order's currency field — confirm via get_lastmile_order if uncertain.")`, `currency: z.string().min(3).max(8).describe("Currency code (e.g., AED, USD).")`, `description: z.string().optional()`, `idempotency_key: z.string().optional()`, `environment: environmentField`.
    - description (rich): "Create a one-off charge against an order — e.g., extra weight surcharge, COD adjustment, customer-reimbursement. Per-order scope-checked. Amount is capped at 100,000 in the chosen currency to prevent agent abuse. Returns the created charge resource."
    - guardrails: `{ rateLimit: { capacity: 5, refillPerSec: 5 / 60 }, idempotency: { keyArg: "idempotency_key", ttlMs: 15 * 60 * 1000 }, audit: true }`.
    - handler: auth → assertOrderBelongsToUser → mint JWT → PlatformApiClient POST `/quiqdash/order-charge` with body `{ order_id, amount, currency, description }`.

    Create `lib/tools/update-order-weight.ts`:
    - inputSchema: `order_id: z.string().min(1)`, `weight_kg: z.number().positive().max(1000).describe("Order weight in kilograms. Must be positive and <= 1000 (sanity cap per T-04 threat register; downstream pricing/SLA logic breaks on negative or absurd values).")`, `idempotency_key: z.string().optional()`, `environment: environmentField`.
    - description: "Update the weight (kg) on a single order. Per-order scope-checked. Weight must be > 0 and <= 1000 — outside this range is rejected client-side per the T-04 threat register. Affects downstream pricing/SLA calculations."
    - guardrails: `{ rateLimit: { capacity: 10, refillPerSec: 10 / 60 }, idempotency: { keyArg: "idempotency_key", ttlMs: 15 * 60 * 1000 }, audit: true }`.
    - handler: auth → assertOrderBelongsToUser → mint JWT → PlatformApiClient PATCH `/quiqdash/orders/${encodeURIComponent(args.order_id)}/weight` with body `{ weight: args.weight_kg }` (or `{ weight_kg }` — pick the wire-format key the upstream actually accepts; consult source-doc; document the choice in the handler comment per the 03-03 wire-format-translation precedent).

    Register all 4 single-order-mutation tools in `app/[transport]/route.ts` under a new block: `// -- Phase 4 Wave 3: Single-order mutations (ORDS-03/04/06/07) --` with 4 imports + 4 registerTool calls, placed AFTER the Wave-2 block.

    Update `evals/snapshots/tool-surface.json` — add 4 alphabetically-sorted entries (create_order_charge, export_order, update_fulfilment_order_status, update_order_weight), each `enabled: true`. Bump enabled count by 4.

    Write `tests/tools/single-order-mutations.test.ts` with 4 describe blocks (one per tool), covering all 13 behavior tests above (~40 assertions total).
  </action>
  <verify>
    <automated>pnpm vitest run tests/tools/single-order-mutations.test.ts && EVAL_GATE=1 bun run eval:tool-surface</automated>
  </verify>
  <acceptance_criteria>
    - `ls lib/tools/create-order-charge.ts lib/tools/update-order-weight.ts` returns both files
    - `grep -c 'z.number().positive().max(100_000)\|z.number().positive().max(100000)' lib/tools/create-order-charge.ts >= 1`
    - `grep -c 'z.number().positive().max(1000)' lib/tools/update-order-weight.ts >= 1`
    - `grep -c 'assertOrderBelongsToUser' lib/tools/create-order-charge.ts == 1`
    - `grep -c 'assertOrderBelongsToUser' lib/tools/update-order-weight.ts == 1`
    - `grep -c '"create_order_charge"\|"export_order"\|"update_fulfilment_order_status"\|"update_order_weight"' evals/snapshots/tool-surface.json >= 4`
    - `grep -c 'Phase 4 Wave 3' app/\[transport\]/route.ts >= 1`
    - `pnpm vitest run tests/tools/single-order-mutations.test.ts` exits 0
    - `EVAL_GATE=1 bun run eval:tool-surface` exits 0
    - `pnpm tsc --noEmit` exits 0
    - Behavior: `create_order_charge` with `amount: -1` rejects at safeParse (returns non-success Zod result)
    - Behavior: `update_order_weight` with `weight_kg: 1001` rejects at safeParse
  </acceptance_criteria>
  <done>All 4 single-order-mutation tools shipped, registered, snapshot updated; numeric bounds enforced at the schema layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-staging CALL-LOG.md for Wave 3 (4 tools)</name>
  <files>.planning/phases/04-orders-write-path-lifecycle/CALL-LOG-wave-03.md</files>
  <read_first>
    - AGENTS.md (live-staging non-negotiable rule)
    - .planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md decision 4
  </read_first>
  <what-built>
    4 single-order mutations: 1 Quiqup-REST PUT (export_order), 1 Platform PATCH with destructive gate (update_fulfilment_order_status), 1 Platform POST (create_order_charge), 1 Platform PATCH with numeric bounds (update_order_weight). MSW tests pass. Live calls required by AGENTS.md.
  </what-built>
  <how-to-verify>
    1. Boot dev server, mint staging JWT.
    2. `export_order`: fire against a real staging order; capture verbatim request + response. Confirm the upstream actually accepts the empty body (or document required body shape if 4xx and adjust the tool).
    3. `update_fulfilment_order_status`: fire with `confirm: true, dry_run: true` first; capture dry-run shape. Then fire LIVE against an order in a safe-to-mutate state with a status that's a no-op-ish change (e.g., re-set to current status); capture response.
    4. `create_order_charge`: fire with a small amount (e.g., 1 AED) against a staging test order; capture response. Verify the BE responded with a created-charge resource id.
    5. `update_order_weight`: fire with a sane weight (e.g., 2.5) against a staging order; verify response payload contains the new weight.
    6. Create `.planning/phases/04-orders-write-path-lifecycle/CALL-LOG-wave-03.md` with one `## <tool_name>` section per tool, request + response or error-name + status + diagnosis.
    7. If any tool surfaces an unlabelled transport error: diagnose at code level, fix, reference commit in CALL-LOG.
  </how-to-verify>
  <resume-signal>Type "approved" once CALL-LOG-wave-03.md captures verbatim live responses for all 4 tools (and any code-level diagnosis if a transport error surfaced).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM caller → MCP handler | `amount`, `weight_kg`, `status`, `order_id` cross here |
| MCP handler → Platform / Quiqup REST | Outbound PATCH/POST/PUT carrying caller-supplied numeric + string fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | create_order_charge runaway amount | mitigate | `z.number().positive().max(100_000)` client-side cap; description warns of the cap; audit on |
| T-04-14 | Tampering | update_order_weight absurd value | mitigate | `z.number().positive().max(1000)` kg range; rejects 0, negative, >1000 at schema-parse |
| T-04-15 | Tampering | update_fulfilment_order_status as confirm-bypass | mitigate | D-06 lockup: tool is DESTRUCTIVE-gated; canonical confirm + dry_run via direct destructive.ts imports |
| T-04-16 | Elevation of Privilege | cross-tenant export/charge/weight | mitigate | `assertOrderBelongsToUser` BEFORE the mutation on every tool |
| T-04-17 | Spoofing | auth ordering | mitigate | `!auth.userId` throw BEFORE requireConfirm (for ORDS-04) and BEFORE assertOrderBelongsToUser (all four) |
| T-04-18 | Tampering | path injection on order_id | mitigate | `encodeURIComponent(args.order_id)` on every URL-path interpolation |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0.
- `pnpm vitest run tests/tools/single-order-mutations.test.ts` passes ~40 assertions.
- `EVAL_GATE=1 bun run eval:tool-surface` exits 0.
- `.planning/phases/04-orders-write-path-lifecycle/CALL-LOG-wave-03.md` exists with verbatim live responses for all 4 tools (AGENTS.md compliance).
- Full `pnpm test` passes with no regressions.
</verification>

<success_criteria>
- All 4 single-order mutations live and registered.
- Numeric bounds (T-04-13/14) enforced at Zod-parse, NOT trusting BE.
- ORDS-04 is the ONLY destructive-gated tool in this wave (D-06).
- All 4 tools carry per-order scope assertion.
</success_criteria>

<output>
Create `.planning/phases/04-orders-write-path-lifecycle/04-03-SUMMARY.md` when done — list 4 shipped tools and any staging order ids used in the live calls.
</output>
