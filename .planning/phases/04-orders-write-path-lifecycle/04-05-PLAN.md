---
phase: 04-orders-write-path-lifecycle
plan: 05
type: execute
wave: 5
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - evals/datasets/batch-transitions-v1.ts
  - evals/batch-transitions.ts
  - evals/score-batch-transitions.ts
  - evals/datasets/single-order-mutations-v1.ts
  - evals/single-order-mutations.ts
  - evals/score-single-order-mutations.ts
  - evals/datasets/order-creation-v1.ts
  - evals/order-creation.ts
  - evals/score-order-creation.ts
  - evals/datasets/missions-v1.ts
  - evals/missions.ts
  - evals/score-missions.ts
  - package.json
  - .github/workflows/eval-gate.yml
autonomous: true
requirements: []
# NOTE: Wave 5 is eval-only — no NEW tool requirements ship here.
# All 20 Phase-4 requirements (ORDS-03/04/06/07, ORDC-04/05, ORDT-03..14, MISS-01/02)
# are covered by plans 04-01..04-04. This wave LOCKS them at the CI layer.

must_haves:
  truths:
    - "4 new Langfuse evals exist: batch-transitions (covers all 12 ORDT tools — they share destructive-gate prompt language), single-order-mutations (4 ORDS tools), order-creation (2 ORDC tools), missions (2 MISS tools)."
    - "Each eval's scorers import `spec.description` / `spec.inputSchema` / `spec.name` DIRECTLY from the production module (drift-proof — eval-time and production description cannot diverge)."
    - "STATIC scorer `destructive-gate-present` asserts the canonical confirm-field language is present on EVERY destructive Phase-4 tool (12 ORDT + update_fulfilment_order_status + transfer_mission_orders + unpool_order = 15 tools)."
    - "STATIC scorer `reason-field-pin` asserts the 4 reason-bearing tools' descriptions contain the relevant `list_*_reasons` tool name (set_on_hold → list_on_hold_reasons, etc)."
    - "STATIC scorer `dry-run-richness` asserts the dry-run code path produces a response with `dryRun: true` flag AND `orderIds`/`orderUuid`/`missionId` fields."
    - "STATIC scorer `factory-uniformity` imports each of the 12 ORDT specs and asserts they share the canonical guardrails block (rateLimit 3/min, idempotency 15m, audit true) — locks the 'all 12 batch transitions go through the factory' invariant at CI."
    - "4 new pnpm scripts (`eval:batch-transitions`, `eval:single-order-mutations`, `eval:order-creation`, `eval:missions`) + 4 new parallel CI jobs in `.github/workflows/eval-gate.yml` with `EVAL_GATE: '1'`."
  artifacts:
    - path: "evals/batch-transitions.ts"
      provides: "Langfuse eval runner for all 12 ORDT batch + single-id transition tools"
      contains: "factory-uniformity"
    - path: "evals/single-order-mutations.ts"
      provides: "Eval runner for the 4 ORDS Phase-4 tools"
      contains: "destructive-gate-present"
    - path: "evals/order-creation.ts"
      provides: "Eval runner for create_internal_fulfilment_order + bulk_create_orders"
      contains: "BL-04"
    - path: "evals/missions.ts"
      provides: "Eval runner for create_mission + transfer_mission_orders"
      contains: "transfer_mission_orders"
    - path: ".github/workflows/eval-gate.yml"
      provides: "CI gate blocking on Phase-4 eval regressions"
      contains: "eval:batch-transitions"
  key_links:
    - from: "evals/score-batch-transitions.ts"
      to: "lib/tools/set-collected.ts ... lib/tools/unpool-order.ts (12 production modules)"
      via: "import spec.description / spec.inputSchema / spec.name + guardrails directly"
      pattern: "import .* from .@/lib/tools/set-"
    - from: ".github/workflows/eval-gate.yml"
      to: "package.json (eval scripts)"
      via: "4 new parallel jobs, each invokes `pnpm run eval:<name>` with EVAL_GATE=1"
      pattern: "EVAL_GATE: \"1\""
---

<objective>
Lock the Phase-4 surface at the CI layer via Langfuse evals. Mirrors the canonical Phase-N final-wave pattern (03-05) — STATIC scorers + drift-proof spec imports + new CI jobs.

Purpose: Phase 4 introduces NO new service hosts (all tools reuse Phase 1-3 clients), so the eval coverage is per-tool-FAMILY (batch-transitions / single-order-mutations / order-creation / missions). Without these scorers, a maintainer could write a 13th transition tool inline (bypassing the factory) and the destructive contract would silently drift — the factory-uniformity scorer catches that.

Output: 4 new eval runners + 4 score modules + 4 datasets + 4 pnpm scripts + 4 CI jobs. No new MCP tools — Wave 5 is eval-only.
</objective>

<execution_context>
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svetoslavdimitrov/Documents/quiqup-mcp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md
@.planning/phases/03-orders-read-path/03-05-SUMMARY.md
@AGENTS.md
@evals/score-orders-document-upload.ts
@evals/score-orders-export.ts
@evals/score-destructive-integrations.ts
</context>

<interfaces>
From `evals/score-orders-document-upload.ts` (canonical structural-assertion scorer pattern from 03-05):
- `import { spec } from "@/lib/tools/upload-order-document"` then assert on `spec.inputSchema.shape` / `spec.guardrails`.

From `evals/score-destructive-integrations.ts` (canonical destructive-gate scorer from 02-06):
- `confirm-gate-present` — asserts confirm field with the canonical description regex in every gated tool's input schema.

From `evals/score-orders-export.ts` (canonical source-inspection scorer from 03-05):
- readFile + comment-strip + substring check pattern.

From `.github/workflows/eval-gate.yml` (existing CI gate from 03-05):
- Existing pattern: 4 Phase-3 jobs each with `EVAL_GATE: "1"`, Anthropic + Langfuse secret mounts.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch-transitions eval (12 tools, factory-uniformity + destructive-gate scorers)</name>
  <files>evals/datasets/batch-transitions-v1.ts, evals/batch-transitions.ts, evals/score-batch-transitions.ts</files>
  <read_first>
    - evals/score-destructive-integrations.ts (canonical confirm-gate-present scorer — 02-06 pattern)
    - evals/score-orders-document-upload.ts (canonical structural-assertion scorer — 03-05 pattern, no-caller-identity-fields + guardrails-block-present)
    - evals/score-orders-history-and-audit.ts (canonical multi-tool eval pattern — 03-05)
    - lib/tools/_batch-transition-factory.ts (Wave 1 output)
    - lib/tools/set-collected.ts and the 11 sibling files (verify they all use the factory)
    - lib/tools/unpool-order.ts (the single-order tool — its guardrails should match canonical destructive block; eval covers it too)
    - .planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md (Wave 5 eval families specification)
  </read_first>
  <behavior>
    - Dataset: 8-10 prompts spanning the 12 transitions. Mix happy-path "transition these N orders to <state>", reason-bearing "place orders X and Y on hold because the customer wasn't home" (agent should pick set_on_hold and supply a reason), exception-path "delivery failed for order Z with reason: address wrong" (agent picks set_delivery_failed + reason), single-order "unpool order X from its mission" (agent picks unpool_order). Each prompt's `expected.tool` is the correct tool name (or array for disambiguation prompts).
    - Eval runner: imports spec.description / spec.inputSchema / spec.name DIRECTLY from each of the 12 production modules (drift-proof). System prompt explicitly instructs the agent on the confirm/dry_run gate and reason-field discoverability.
    - Scorer `tool-name-match`: standard.
    - Scorer `args-overlap`: standard.
    - Scorer `description-quality`: STATIC source-inspection — for each of 12 tools, assert spec.description length > 100 chars AND contains "DESTRUCTIVE" AND contains "confirm: true".
    - Scorer `destructive-gate-present`: STATIC structural-assertion — for each of the 15 destructive Phase-4 tools (12 ORDT + update_fulfilment_order_status + transfer_mission_orders + unpool_order, the latter 3 imported once Wave 2/3/4 ships) assert `spec.inputSchema.shape.confirm` exists AND its description starts with "DESTRUCTIVE-GATE:". Score 1.0 if all pass, 0.0 if any fail.
    - Scorer `factory-uniformity`: STATIC structural-assertion — import all 12 ORDT specs, assert each one's `guardrails` block equals `{ rateLimit: { capacity: 3, refillPerSec: 3/60 }, idempotency: { keyArg: "idempotency_key", ttlMs: 900000 }, audit: true }` (deep equality OR field-by-field). This is the critical "no inline bypass" scorer — a 13th transition tool written inline (NOT via factory) would either omit a guardrail or set different values, and this scorer fires.
    - Scorer `reason-field-pin`: STATIC — for each of the 4 reason-bearing tools (set_on_hold, set_return_to_origin, set_delivery_failed, set_collection_failed), assert spec.inputSchema.shape contains `reason` AND spec.description contains the relevant `list_*_reasons` substring.
    - Scorer `dry-run-richness`: STATIC — for ONE representative tool (set_collected), invoke the handler with `confirm: true` + `dry_run: true` + a mocked auth context + mocked assertOrderBelongsToUser; parse the returned JSON; assert it contains `dryRun: true` AND `orderIds: [...]`. This is the only behavioral scorer in the static suite.
  </behavior>
  <action>
    Create `evals/datasets/batch-transitions-v1.ts` exporting `export const dataset = [ { input: { prompt: "...", system: "..." }, expected: { tool: "set_collected", required: ["order_ids"] } }, ... ]` with 8-10 items.

    Create `evals/batch-transitions.ts` as the runner — imports all 12 specs at the top, registers them as the candidate tool set, system prompt explains the destructive gate + dry-run + reason-field discoverability.

    Create `evals/score-batch-transitions.ts` exporting the 6 scorers above. For `factory-uniformity`, use deep equality via JSON.stringify of the guardrails block (or write a small assertGuardrailsBlock helper). For `dry-run-richness`, mock `@/lib/middleware/scope` (`vi.mock` won't work outside vitest, so use a simpler approach: directly construct mock args + auth and call the handler; if the handler depends on the actual `getQuiqupReadyJwt`, intercept at the http layer with MSW — or, simpler, skip the handler-invocation test and instead grep the factory source for the `dryRun: true` literal as a static assertion).

    Threshold convention (mirrors 03-05): tool-name-match ≥ 0.75, args-overlap ≥ 0.7, description-quality ≥ 1.0, destructive-gate-present ≥ 1.0, factory-uniformity ≥ 1.0, reason-field-pin ≥ 1.0, dry-run-richness ≥ 1.0.
  </action>
  <verify>
    <automated>EVAL_DRY_RUN=1 pnpm run eval:batch-transitions</automated>
  </verify>
  <acceptance_criteria>
    - `ls evals/batch-transitions.ts evals/score-batch-transitions.ts evals/datasets/batch-transitions-v1.ts` returns all 3
    - `grep -c 'from "@/lib/tools/set-' evals/batch-transitions.ts >= 11` (imports all 12 transition specs; minus 1 if the file uses a wildcard or named array — adjust if so)
    - `grep -c 'factory-uniformity\|destructive-gate-present\|reason-field-pin\|dry-run-richness' evals/score-batch-transitions.ts >= 4` (all 4 new STATIC scorers defined)
    - `grep -c 'list_on_hold_reasons\|list_return_to_origin_reasons\|list_courier_failure_reasons' evals/score-batch-transitions.ts >= 3` (reason-field-pin checks these literals)
    - `EVAL_DRY_RUN=1 pnpm run eval:batch-transitions` exits 0 — UNTIL the script exists in package.json (this verify command only passes AFTER Task 4 wires the script; mark this as "deferred verify" in the execution log, OR run the runner directly via `EVAL_DRY_RUN=1 npx tsx evals/batch-transitions.ts`)
    - All 4 new STATIC scorers return value=1.0 against current production source (factory exists, all 12 tools use it, reason fields pinned, dry-run shape present)
  </acceptance_criteria>
  <done>Batch-transitions eval runner + score module + dataset compile and all STATIC scorers return 1.0 against the production tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-order-mutations eval (4 tools) + Order-creation eval (2 tools)</name>
  <files>evals/datasets/single-order-mutations-v1.ts, evals/single-order-mutations.ts, evals/score-single-order-mutations.ts, evals/datasets/order-creation-v1.ts, evals/order-creation.ts, evals/score-order-creation.ts</files>
  <read_first>
    - evals/score-orders-document-upload.ts (canonical structural-assertion pattern — guardrails-block-present, no-caller-identity-fields)
    - evals/score-orders-export.ts (canonical source-inspection pattern — readFile + substring assertions)
    - lib/tools/export-order.ts, lib/tools/update-fulfilment-order-status.ts, lib/tools/create-order-charge.ts, lib/tools/update-order-weight.ts (Wave 3 outputs)
    - lib/tools/create-internal-fulfilment-order.ts, lib/tools/bulk-create-orders.ts (Wave 4 outputs)
    - .planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md threat_model_guidance (numeric-bound + bulk-row-error scorers)
  </read_first>
  <behavior>
    Single-order-mutations eval:
    - Dataset: 6-8 prompts spanning the 4 tools. e.g., "export order X" → export_order; "set fulfilment order Y to status shipped" → update_fulfilment_order_status (the destructive one); "charge 50 AED to order Z for additional weight" → create_order_charge; "the actual weight of order W is 5.2 kg, update it" → update_order_weight.
    - Scorers: tool-name-match, args-overlap, description-quality, destructive-gate-present-ORDS-04 (only update_fulfilment_order_status is destructive per D-06), numeric-bounds-pin (STATIC: readFile create-order-charge.ts + assert `100_000` substring present; readFile update-order-weight.ts + assert `1000` substring present — locks the T-04-13/14 bounds).
    - Thresholds same as Task 1.

    Order-creation eval:
    - Dataset: 5-6 prompts. e.g., "create an internal fulfilment order for SKU X qty 2 to address Y" → create_internal_fulfilment_order; "I have a CSV of 50 orders to upload, here's the file as base64: ..." → bulk_create_orders; NEGATIVE prompt where user asks the agent to pass user_id (BL-04 honor) → agent should ignore the field.
    - Scorers: tool-name-match, args-overlap, description-quality, bl-04-server-binding (STATIC structural: for both tools, assert NONE of `user_id, actor_id, actor_email, partner_id, uploader_id` are in `Object.keys(spec.inputSchema.shape)`). For bulk_create_orders, ALSO assert the 13_500_000 size cap is present in spec.description OR inputSchema (readFile substring).
    - Thresholds same.
  </behavior>
  <action>
    For single-order-mutations:
    - `evals/datasets/single-order-mutations-v1.ts` — dataset of 6-8 items.
    - `evals/single-order-mutations.ts` — runner importing all 4 specs.
    - `evals/score-single-order-mutations.ts` — scorers including `numeric-bounds-pin` (readFile create-order-charge.ts + assert `/\.max\(100_?000\)/` regex match; readFile update-order-weight.ts + assert `/\.max\(1000\)/`).

    For order-creation:
    - `evals/datasets/order-creation-v1.ts` — dataset of 5-6 items INCLUDING the BL-04 negative prompt.
    - `evals/order-creation.ts` — runner importing both specs.
    - `evals/score-order-creation.ts` — scorers including `bl-04-server-binding` (import spec, assert `Object.keys(spec.inputSchema.shape)` contains NONE of the identity-field names — mirrors the no-caller-identity-fields scorer from 03-05) AND `bulk-csv-cap-pin` (readFile bulk-create-orders.ts + assert `/13_?500_?000/`).
  </action>
  <verify>
    <automated>EVAL_DRY_RUN=1 npx tsx evals/single-order-mutations.ts && EVAL_DRY_RUN=1 npx tsx evals/order-creation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls evals/single-order-mutations.ts evals/score-single-order-mutations.ts evals/datasets/single-order-mutations-v1.ts` returns all 3
    - `ls evals/order-creation.ts evals/score-order-creation.ts evals/datasets/order-creation-v1.ts` returns all 3
    - `grep -c 'numeric-bounds-pin' evals/score-single-order-mutations.ts >= 1`
    - `grep -c 'bl-04-server-binding\|no-caller-identity' evals/score-order-creation.ts >= 1`
    - `grep -c 'bulk-csv-cap-pin\|13_?500_?000' evals/score-order-creation.ts >= 1`
    - Both runners exit 0 in dry-run mode
    - All 4 new STATIC scorers return value=1.0 against current production source
  </acceptance_criteria>
  <done>Both evals compile, STATIC scorers verify against the production tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Missions eval (2 tools) — locks create-vs-transfer gating asymmetry</name>
  <files>evals/datasets/missions-v1.ts, evals/missions.ts, evals/score-missions.ts</files>
  <read_first>
    - lib/tools/create-mission.ts, lib/tools/transfer-mission-orders.ts (Wave 4 outputs)
    - evals/score-destructive-integrations.ts (canonical confirm-gate-present)
    - .planning/phases/04-orders-write-path-lifecycle/04-CONTEXT.md decision 5 (create=NOT gated; transfer=DESTRUCTIVE)
  </read_first>
  <behavior>
    - Dataset: 4-5 prompts. e.g., "create a new delivery mission for Dubai depot scheduled for tomorrow" → create_mission; "move orders A, B, C into mission M" → transfer_mission_orders (agent should ALSO recognize the confirm:true requirement); disambiguation prompt "I want to set up a mission with these 3 orders" — could be create_mission OR create+transfer in sequence (expected: tool=[create_mission] with a note acknowledging the follow-up).
    - Scorers: tool-name-match, args-overlap, description-quality, gating-asymmetry-lock (STATIC structural: assert `create_mission.inputSchema.shape` does NOT contain `confirm`, AND `transfer_mission_orders.inputSchema.shape` DOES contain `confirm` with the destructive-gate description). This is the critical D-05 lock — a maintainer who flips either gating decision would trip this scorer.
    - destructive-gate-present for transfer_mission_orders (mirrors batch-transitions' scorer but scoped to just this tool).
  </behavior>
  <action>
    Create `evals/datasets/missions-v1.ts` (4-5 items), `evals/missions.ts` (runner), `evals/score-missions.ts` (scorers).

    The `gating-asymmetry-lock` scorer body: import both specs, then `assert !("confirm" in createMissionSpec.inputSchema.shape) && "confirm" in transferMissionOrdersSpec.inputSchema.shape && transferMissionOrdersSpec.inputSchema.shape.confirm._def.description.startsWith("DESTRUCTIVE-GATE:")`. Score 1.0 if all three hold, 0.0 otherwise. Include a comment in the scorer body explaining that this is the D-05 lock and which decision flip would trip it.
  </action>
  <verify>
    <automated>EVAL_DRY_RUN=1 npx tsx evals/missions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls evals/missions.ts evals/score-missions.ts evals/datasets/missions-v1.ts` returns all 3
    - `grep -c 'gating-asymmetry-lock\|D-05' evals/score-missions.ts >= 1`
    - `grep -c 'create_mission\|transfer_mission_orders' evals/missions.ts >= 2` (both specs imported)
    - Runner exits 0 in dry-run mode
    - `gating-asymmetry-lock` returns 1.0 against current production source (create_mission has no confirm; transfer_mission_orders has the canonical destructive confirm)
  </acceptance_criteria>
  <done>Missions eval compiles; D-05 gating asymmetry locked at the CI layer.</done>
</task>

<task type="auto">
  <name>Task 4: Wire 4 new pnpm scripts + 4 new parallel CI jobs in eval-gate.yml</name>
  <files>package.json, .github/workflows/eval-gate.yml</files>
  <read_first>
    - package.json (existing `eval:*` scripts — add the 4 new ones immediately after the Phase-3 final entry, alphabetical placement OK)
    - .github/workflows/eval-gate.yml (existing structure — 4 Phase-3 jobs each with EVAL_GATE: "1", secret mounts, pnpm install + run; replicate the shape)
    - .planning/phases/03-orders-read-path/03-05-SUMMARY.md (canonical Phase-N final-wave wiring pattern)
  </read_first>
  <behavior>
    - `package.json` has 4 new scripts: `eval:batch-transitions`, `eval:single-order-mutations`, `eval:order-creation`, `eval:missions`. Each runs `tsx evals/<name>.ts`.
    - `.github/workflows/eval-gate.yml` has 4 new parallel jobs (each in `jobs:` block), each with EVAL_GATE: "1" env, ANTHROPIC_API_KEY + LANGFUSE_* secret mounts, `pnpm install && pnpm run eval:<name>`.
    - YAML well-formed: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/eval-gate.yml'))"` exits 0.
  </behavior>
  <action>
    Edit `package.json` — under the `scripts` block, after `eval:orders-document-upload` (the Phase-3 last entry), add the 4 new entries:
    ```
    "eval:batch-transitions": "tsx evals/batch-transitions.ts",
    "eval:single-order-mutations": "tsx evals/single-order-mutations.ts",
    "eval:order-creation": "tsx evals/order-creation.ts",
    "eval:missions": "tsx evals/missions.ts",
    ```

    Edit `.github/workflows/eval-gate.yml` — under `jobs:`, add 4 new jobs after the Phase-3 jobs. Each follows the shape:
    ```
    eval-batch-transitions:
      runs-on: ubuntu-latest
      steps:
        - <checkout, setup-node, install pnpm, install deps>
        - run: pnpm run eval:batch-transitions
          env:
            EVAL_GATE: "1"
            ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
            LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
            LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
            LANGFUSE_BASE_URL: ${{ secrets.LANGFUSE_BASE_URL }}
    ```
    Repeat for the other 3.

    Update the file-header comment to document Phase-4 coverage.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/eval-gate.yml'))" && pnpm run eval:batch-transitions --help 2>&1 | grep -q tsx || EVAL_DRY_RUN=1 pnpm run eval:batch-transitions</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"eval:batch-transitions"' package.json == 1`
    - `grep -c '"eval:single-order-mutations"' package.json == 1`
    - `grep -c '"eval:order-creation"' package.json == 1`
    - `grep -c '"eval:missions"' package.json == 1`
    - `grep -c 'eval-batch-transitions:\|eval-single-order-mutations:\|eval-order-creation:\|eval-missions:' .github/workflows/eval-gate.yml >= 4`
    - `grep -c 'EVAL_GATE: "1"' .github/workflows/eval-gate.yml >= 8` (4 Phase-3 + 4 Phase-4 jobs)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/eval-gate.yml'))"` exits 0
    - `EVAL_DRY_RUN=1 pnpm run eval:batch-transitions` exits 0
    - `EVAL_DRY_RUN=1 pnpm run eval:single-order-mutations` exits 0
    - `EVAL_DRY_RUN=1 pnpm run eval:order-creation` exits 0
    - `EVAL_DRY_RUN=1 pnpm run eval:missions` exits 0
  </acceptance_criteria>
  <done>4 new eval scripts wired in package.json; 4 new CI jobs added to eval-gate.yml; YAML well-formed; all 4 scripts exit 0 in dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production tools → eval scorers | Scorers read spec.description / spec.inputSchema / spec.guardrails directly from production modules — drift-proof |
| CI gate → maintainer commits | EVAL_GATE=1 blocks merge on any STATIC scorer regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering | Inline 13th transition bypassing factory | mitigate | `factory-uniformity` scorer imports all 12 ORDT specs and asserts identical guardrails block; a 13th tool written inline with different guardrails fails CI |
| T-04-27 | Tampering | D-05 gating flip (create_mission becomes destructive, transfer becomes non-gated) | mitigate | `gating-asymmetry-lock` scorer asserts the exact create=non-gated / transfer=destructive shape |
| T-04-28 | Tampering | D-06 update_fulfilment_order_status loses confirm gate | mitigate | `destructive-gate-present` scorer covers all 15 Phase-4 destructive tools including ORDS-04 |
| T-04-29 | Tampering | D-02 reason field replaced with z.enum (drift surface) | mitigate | `reason-field-pin` scorer asserts the 4 reason-bearing tools' descriptions name the relevant `list_*_reasons` tool; replacing with z.enum would not necessarily fail this — also assert `inputSchema.shape.reason._def.typeName === "ZodString"` to lock the free-form shape |
| T-04-30 | Tampering | BL-04 server-binding regression on creation tools | mitigate | `bl-04-server-binding` scorer asserts none of user_id/actor_id/etc. in shape (mirrors no-caller-identity-fields from 03-05) |
| T-04-31 | Tampering | bulk_create_orders 10MB cap regression | mitigate | `bulk-csv-cap-pin` scorer asserts the 13_500_000 literal present |
| T-04-32 | Tampering | numeric-bound regression on create_order_charge / update_order_weight | mitigate | `numeric-bounds-pin` scorer asserts the 100_000 / 1000 literals present |
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0.
- All 4 new evals exit 0 in `EVAL_DRY_RUN=1` mode.
- All ~8 new STATIC scorers return value=1.0 against the current production tree (verify in-process by invoking each scorer with the production spec).
- `.github/workflows/eval-gate.yml` is YAML-well-formed and has 4 new parallel jobs.
- `pnpm test` full suite passes with no regressions.
- The Phase-4 eval-coverage invariant (per PROJECT.md) is satisfied at the family-grouping level: batch-transitions / single-order-mutations / order-creation / missions.
</verification>

<success_criteria>
- 4 Langfuse evals exist with drift-proof spec imports.
- 4 STATIC structural-assertion scorers lock the critical Phase-4 invariants (factory uniformity, gating asymmetry, BL-04 server-binding, numeric bounds).
- CI gate fails on any of those regressions.
- Phase 4 is now complete — 5/5 plans shipped.
</success_criteria>

<output>
Create `.planning/phases/04-orders-write-path-lifecycle/04-05-SUMMARY.md` when done — list shipped eval artifacts, scorer-result table (each scorer + value=1.0 verification), and the project-status follow-up (flag for user: PROJECT.md per-family eval invariant row update for Phase 4).
</output>
